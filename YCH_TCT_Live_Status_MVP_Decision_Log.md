# Live Status MVP Decision Log

This append-only log records decisions applied to `YCH_TCT_Live_Status_MVP.html`. It supports traceability and reversal. These entries describe mockup decisions and are not confirmed YCH operational requirements unless an authoritative source says so.

## Logging Rules

- Use IDs in the form `LS-D###`.
- Append a new entry when a decision is implemented or explicitly accepted.
- Never delete or rewrite an old decision. Record a reversal as a new decision that supersedes the earlier ID.
- State the previous behavior and a practical revert instruction.
- Link each decision to its backlog ID when one exists.
- Record conflicts with the project knowledge base instead of silently reconciling them.

## Current Baseline

| ID | Date | Decision | Previous behavior | Revert guidance | Source |
|---|---|---|---|---|---|
| LS-D001 | 2026-08-27 | Live Status provides separate Trips and Orders perspectives. | One execution view did not clearly separate the counting unit. | Remove the perspective switch and retain the required entity view. | Explicit user decision |
| LS-D002 | 2026-08-27 | A trip may carry multiple orders for one ship-to. | Shared trips could contain several ship-tos. | Restore per-order ship-tos within a trip and plural ship-to presentation. | Explicit user decision; conflicts with the KB multi-drop example |
| LS-D003 | 2026-08-27 | Trip stages are All active, Awaiting Dispatch, In Transit and At Destination. Return is excluded from active lists and counts. | Delivered and Return were separate Trip filters. | Restore the Return chip and Return records, then rename At Destination to Delivered if required. | Explicit user decision |
| LS-D004 | 2026-08-27 | Vehicle, carrier and transport-status fields are omitted from the Live Status tables. | These appeared as columns, filters and detail fields. | Restore the fields from the retained mock data and reconnect the carrier filter. | Explicit user decision |
| LS-D005 | 2026-08-27 | Ship-to rows show one name and a compact MT, GT, SP or Export trade-channel badge. | Customer and ship-to names were repeated in the same row. | Remove the channel field and badge, then restore the preferred customer or ship-to label. | Explicit user decision |
| LS-D006 | 2026-08-27 | Visible times omit WIB, WITA and WIT labels. | Times displayed Indonesian timezone abbreviations. | Remove the display sanitization and restore timezone suffixes where needed. | Explicit user decision |
| LS-D007 | 2026-08-27 | ETA contains the time and relative day. A separate Variance column is not shown. | ETA and Variance were separate columns. | Restore the Variance header and cells, then render the full ETA value. | Explicit user decision |
| LS-D008 | 2026-08-27 | Lateness appears only for arrivals today, only during In Transit or At Destination, and only above 30 minutes. Forecast-late wording is prohibited. | Any positive variance could display as late across all stages and days. | Relax the conditions in `etaParts` and restore the required stage wording. | Explicit user decision |
| LS-D009 | 2026-08-27 | The Trip table shows Exception instead of Affected orders and aggregates distinct exception types. | It showed the number of affected orders. | Restore affected-order counts and their explanatory copy. | Explicit user decision |
| LS-D010 | 2026-08-27 | The Order table mirrors the compact Trip table: entity, linked entity, ship-to, route, ETA, position source and exception. | Current and next milestones occupied the main Order table. | Restore milestone columns to the Order table. | Explicit user decision |
| LS-D011 | 2026-08-27 | Order Details use an eight-step horizontal journey beginning at Truck Checked in and ending at POD Completed. | A vertical internal milestone table included planning, timing-note and country-specific rows. | Restore the milestone table and its required columns and milestones. | Explicit user decision |
| LS-D012 | 2026-08-27 | The Order Journey appears at the top of the detail drawer. Its sequence line is continuous, and its time axis is proportional to elapsed time with progress only to the current milestone. | The journey appeared below other details and used a fully blue, equally spaced time bar. | Move the card below the detail sections and restore equal tick spacing or full progress. | Explicit user decision |
| LS-D013 | 2026-08-27 | The order-trip many-to-many summary banner is omitted. | A banner explained the relationship and displayed counts. | Restore the banner, its responsive styles and count initialization. | Explicit user decision |
| LS-D014 | 2026-08-27 | Ship-to and Route headers in the Trip table do not have explanation markers. | Both headers displayed `!` markers. | Restore the explanation markers and their popover text. | Explicit user decision |
| LS-D015 | 2026-08-27 | The Live Status page does not show the subtitle "Monitor active orders and the trips that execute them across the full journey." | The sentence appeared below the Live Status heading. | Restore the `page-subtitle` paragraph below the Live Status heading. | Explicit user decision |
| LS-D016 | 2026-08-27 | Journey-stage chips, search, source filter, exception filter and view toggle share one horizontally scrollable control row. | Journey-stage chips occupied a separate row above the search, filters and view toggle. | Remove the `controls-row` wrapper and its styles, then restore the original padding and bottom borders on `journey-filter` and `toolbar`. | Explicit user decision |
| LS-D017 | 2026-08-27 | The visible `Showing x of xxx active orders/trips` result counter is omitted, and `Clear filters` sits in the main control row beside the filters. | A separate result row displayed the counter and placed `Clear filters` below the main controls. | Restore the `result-meta` row and `result-count` element, reconnect its JavaScript reference and update, and move `Clear filters` back into that row. | Explicit user decision |
| LS-D018 | 2026-08-27 | The `Live Status` heading and Trips/Orders perspective switch share one horizontal heading row on desktop. | The perspective switch appeared on a separate row below the heading, adding vertical space. | Remove the flex alignment from `page-heading` and restore the top margin on `perspective-row`. | Explicit user decision |
| LS-D019 | 2026-08-27 | The `Illustrative MVP data` label and all circular explanation markers are hidden from the Live Status interface. Operational exception symbols remain visible. | The header labelled the dataset as illustrative, and circular `!` markers opened explanatory popovers throughout the page. | Restore the illustrative-data label and remove the `display: none` rule from `explain-marker`. | Explicit user decision |
| LS-D020 | 2026-08-27 | The Live Status page omits the left navigation sidebar, and the main workspace uses the full available width. | A 224 px sidebar displayed the End-to-End Visibility section, Live Status navigation item and internal-use note. | Restore the sidebar markup and the two-column `workspace` grid with a 224 px first column. | Explicit user decision |
| LS-D021 | 2026-08-27 | The Live Status page omits the `Control Tower > End-to-End Visibility > Live Status` breadcrumb. | The breadcrumb appeared above the page heading and repeated the current navigation context. | Restore the `breadcrumb` element immediately inside `main`, above `page-header`. | Explicit user decision |
| LS-D022 | 2026-08-27 | Order and Trip detail drawers omit the right-side Route progress card, and the live journey map uses the full content width. | The map shared its row with a Route progress card containing percentage, distance, route points and current-exception text. | Restore the two-column `map-layout` grid and remove the forced hidden state from `route-summary`. | Explicit user decision |
| LS-D023 | 2026-08-27 | Detail and network maps use a solid semantic-green line for travelled distance and a dashed Gunmetal line for remaining distance; the legend states both patterns. | Travelled and remaining distance were both solid, using green and amber respectively, so remaining distance could be mistaken for caution. | Restore the solid amber remaining polylines and the original solid amber legend swatch. | Explicit user decision; `KB/UI instruction.md` color and visualization rules |
| LS-D024 | 2026-08-27 | Detail and network map routes use a 4 px main stroke with an 8 px white casing. | Route lines used a 6 px main stroke with a 10 px casing and appeared too visually heavy against the map. | Restore route weights to 6 px and casing weights to 10 px in both map renderers. | Explicit user decision |
| LS-D025 | 2026-08-27 | Maps distinguish a solid green observed trail, a short-dashed amber estimated GPS gap and a long-dashed Gunmetal straight destination direction. The straight segment is not presented as a road route. Computed in-transit mockup records approximate the gap start until an actual last-observed coordinate is supplied. | All past geometry was presented as observed, while remaining geometry was road-routed from the computed or observed current position to the destination. | Remove the display-segment split, restore one travelled polyline, and restore routed remaining geometry in `resolveRoute`. | Explicit user decision; `KB/UI instruction.md` semantic-color and non-color distinction rules |
| LS-D026 | 2026-08-27 | The Network view omits the separate Truck/Ship legend and uses professional inline SVG truck and cargo-vessel markers with white surfaces, Gunmetal outlines, blue selected states and red exception dots. | A floating legend repeated the vehicle types, while yellow Material Symbols represented trucks and ships on the map. | Restore the `fleet-legend` markup and styles, and restore the Material Symbols implementation in `networkIcon`. | Explicit user decision; `KB/UI instruction.md` color, iconography and restrained-interface rules |
| LS-D027 | 2026-08-27 | Order and Trip detail drawers retain the Journey panel above a prominent full-width map and omit all cards below the map. The map uses responsive viewport-based height to occupy the released space. | Entity and position summary cards plus linked Trip or Order records appeared below a fixed-height map. | Restore the below-map detail sections, their styles and population logic, then return `#detail-map` to its previous fixed responsive heights. | Explicit user decision |
| LS-D028 | 2026-08-27 | Trip details retain the Sales Order and Stock Transfer Order list below the prominent map. Order details do not show this Trip-owned list. This refines LS-D027. | LS-D027 removed every below-map section from both detail perspectives. | Remove `trip-orders-card` and its population logic to return to the LS-D027 map-only area below Journey. | Explicit user decision |
| LS-D029 | 2026-08-27 | Three illustrative multi-leg STO/SO cases (`multiLegCases`, trips TR-9301 to TR-9303) show that an order's own journey milestone caps at its own final ship-to, not at the current trip's leg destination. A through-order (marked with `legDestination`) keeps reading `In transit` once its shared trip's leg arrives and its POD is signed, while an order whose ship-to matches that leg's destination reads `POD Completed` together with the trip. Case 1 (Cikarang MDC to Surabaya Hub to Malang DC) and Case 3 (Cikarang MDC to Makassar DC to Parepare Hub to Pinrang Hub, the latter carrying two-trip history) show the state right after arrival; Case 2 (Cikarang MDC to Solo Hub to Yogyakarta DC) shows the state before arrival, when the trip and every order it carries still share one milestone. This refines LS-D002, which restricted trips to one ship-to; these three trips carry two ship-tos each. | Every trip enforced one shared ship-to and one shared milestone set across all its orders (`tripOwnedFields`, `sharedTripGroups`), so an order's milestone could never diverge from its trip's. | Remove `multiLegCases` and the `orders.push(...multiLegCases)` call, and remove the `legDestination` handling in the order table's Route cell, `openTripDrawer`'s orders list, and `openOrderDrawer`'s milestone copy. | Explicit user decision |
| LS-D030 | 2026-08-27 | A fourth illustrative case (Case 4, trip TR-9304, Cikarang MDC to Bandung DC to Garut Hub) shows the mirror of LS-D029: a true multi-drop trip with two physical stops, where the order delivered at the earlier stop (marked with `deliveredEnRoute`) reads `POD Completed` ahead of the trip itself, which stays `In transit` until it reaches its own final stop. This is the reverse of a through-order, which stays behind its trip; a `deliveredEnRoute` order runs ahead of it. | No case showed an order finishing before its own trip. | Remove the two `deliveredEnRoute` order records from `multiLegCases` and the `deliveredEnRoute` handling in the order table's Route cell, `openTripDrawer`'s orders list, and `openOrderDrawer`'s milestone copy. | Explicit user decision, following a brainstorm of further STO/SO cases |
| LS-D031 | 2026-08-28 | The mockup's network, sites and coordinates are re-anchored from Indonesia to Vietnam, shipper Suntory PepsiCo Vietnam Beverage (SPVB), matching the Track 2 scope pivot recorded in `KB/CT_TCT_SPVB_Working_File_v11.md` (VD1 to VD4). `orders`, `generatedRouteTemplates`, `generatedCarriers`, `generatedVehicleTypes`, and the vehicle-plate format are all replaced; `mapMode` is always `Truck` now, since Vietnam is one landmass and the archipelago ferry model does not apply; times carry no WIB/WITA/WIT suffix, since Vietnam runs one timezone. The frozen worked example VN-TR-2214 (VD4) is added as its own order. Milestone 4, `Truck Queue in`, is added to `orderJourneyMilestones` (now nine steps) since it is live for this scope (VD5); it stays out of `spvbJourneyMilestones`, since D84 does not mark it customer-visible. Cases 1 to 4 from LS-D029 and LS-D030 keep their mechanism (`legDestination`, `deliveredEnRoute`) unchanged, re-anchored onto the new network: Case 1 MDC Hoc Mon to HUB Da Nang to Supply point Hai Phong, Case 2 MDC Hoc Mon to Supply point Hanoi to Dong Anh Distributor, Case 3 MDC Hoc Mon to HUB Da Nang to Supply point Quy Nhon to An Nhon Distributor, Case 4 MDC Hoc Mon to YCH DC Thuan An to Supply point Can Tho. The role-switch button previously labelled `SPVB` is relabelled `Customer view`, since the mockup itself is now SPVB-scoped and the two meant different things. The topbar subtitle changes from `Indonesia operations` to `Vietnam operations, SPVB`. | The network was Indonesian (Cikarang, Surabaya, Makassar, and the rest), shipper unnamed on screen, `mapMode` included `Ship` for one archipelago sea leg, times carried WIB/WITA/WIT, `orderJourneyMilestones` had eight steps starting at Truck Checked in, and the role-switch button read `SPVB`. | Restore the Indonesian `orders`, `generatedRouteTemplates`, `generatedCarriers`, `generatedVehicleTypes`, and vehicle-plate format from before this entry; remove `Truck Queue in` from `orderJourneyMilestones` and its `spvbMilestoneMap` entry; restore the WIB/WITA/WIT suffixes; revert the topbar subtitle and the role-switch label. | Explicit user decision; `KB/CT_TCT_SPVB_Working_File_v11.md` VD1 to VD5 |
| LS-D032 | 2026-08-28 | Order and Trip details gain an Exception history card listing every exception raised, snoozed or resolved, not only the current open one. `exceptionHistory` is an authored array on an order (type, severity level, status, raised and updated times, note); where absent, the order's own `exception` field stands in as one Open entry via `exceptionHistoryFor`, so every order shows something truthful without needing per-record authoring. A trip's card aggregates every carried order's history, each entry tagged with its order and a jump-to-order button, mirroring how `trip-orders-card` already works. Three hand-authored orders (SO-260825-1048, SO-260825-1116, SO-260824-0982) carry full authored histories to show the Resolved, Snoozed and reopened-Open states; the rest fall back to the derived single entry. | Order and Trip drawers showed only the single current `exception` and `exceptionLevel`, with no record of what came before it or how it was handled. | Remove the `exception-history-card` markup, the `exceptionHistoryFor`, `exceptionStatusClass` and `renderExceptionHistory` functions, their calls in `openOrderDrawer` and `openTripDrawer`, and the three authored `exceptionHistory` arrays. | Explicit user decision, request relayed from seniors |
| LS-D033 | 2026-08-28 | Exception status is visible without opening a drawer, refining LS-D032 after feedback that checking status per order took too many clicks. Two changes: (1) the Order and Trip tables' Exception column now leads with a status badge (Open, Snoozed or Resolved, via `latestExceptionStatus` and `exceptionCell`) ahead of the exception name, and the exception filter gained Snoozed and Resolved options alongside Open and None; (2) a third perspective, Exceptions, lists every exception across every active order in one flat, searchable, status-filterable table (`allExceptionEntries`, `filteredExceptionEntries`, `renderExceptionsTable`), with its own metric tiles (Total, Open, Snoozed, Resolved) and no Journey stage chips, Network view or source filter, since none apply to an exception event. Row click opens the order. | The Exception column showed only the exception name and its severity color, with no status; the exception filter was a Yes/No exception toggle; there was no cross-order exception list, so seeing status meant opening every order or trip drawer in turn. | Restore the two-branch exception-cell template and the Open/None-only filter in `filteredEntities`; remove the `exceptions-perspective-button`, `allExceptionEntries`, `filteredExceptionEntries`, `renderExceptionsTable`, `updateExceptionMetrics`, `latestExceptionStatus` and `exceptionCell`, and the `isExceptions` branch in `setPerspective`. | Explicit user decision, following feedback that LS-D032 required too many steps to see status |
| LS-D034 | 2026-08-28 | The `Journey stage` label above the stage-filter chips (`All active`, `Awaiting Dispatch`, `In Transit`, `At Destination`) is removed; the chips themselves are unchanged. `configureJourneyStages` no longer sets an explain-marker on this label, since the element is gone. The now-unused `.filter-title` CSS rule is also removed. | The `.journey-filter` row opened with a `Journey stage` text label (plus a hidden explain-marker) before the stage chips. | Restore the `filter-title` span (`Journey stage` text and its explain-marker) before the first stage chip, restore the two `marker` lines in `configureJourneyStages`, and restore the `.filter-title` CSS rule. | Explicit user decision |
| LS-D035 | 2026-08-28 | Every column header, on all three tables (Trips, Orders, Exceptions), is a clickable sort control with an ascending/descending arrow (`sortableHeaderCell`, `bindSortHeaders`). `ORDER_COLUMNS`, `TRIP_COLUMNS` and `EXCEPTION_COLUMNS` define each column's comparable value; ETA and Raised sort chronologically via `sortableMinutes` (parses the same "DD Mon, HH:MM" pattern used elsewhere) rather than as display text, and Exception and Status sort by urgency (Open, then Snoozed, then Resolved, then none) via `exceptionSortRank` rather than alphabetically. `applySort` is shared by `filteredEntities` and `filteredExceptionEntries`. Sort resets to none on every perspective switch and on Clear filters, since the column set changes. The now-unused `explanationMarker` function and the (already hidden, per LS-D019) explain-markers it produced in the Trips/Orders table headers are removed, since header cells are now sort buttons. | Table headers were plain text with no sort affordance; three of the seven Trips/Orders header cells additionally carried a hidden explain-marker. | Restore the plain `<th>` header strings (with `explanationMarker` calls for Trip, Position source and Exception) in `renderTable` and `renderExceptionsTable`; remove `sortableHeaderCell`, `bindSortHeaders`, `applySort`, `sortableMinutes`, `exceptionSortRank`, the three `*_COLUMNS` arrays, and `sortKey`/`sortDirection` from `state`. | Explicit user decision |
| LS-D036 | 2026-08-28 | The Network view's Active trips/orders sidebar gains its own search box (`lane-search`) next to its heading, three-way synchronised with `table-search` and `global-search` (`els.laneSearch`, same `state.query`). `#network-map` and `.lane-list` change from a fixed `520px`/`min-height: 380px` to the same responsive `clamp()` viewport-based height already used for `#detail-map` (LS-D027), at all three breakpoints, so the map and sidebar grow to fill the space `.workspace`'s `min-height: calc(100vh - 56px)` releases below the panel instead of leaving it as blank background. | The Network view had no local search control, relying only on the shared toolbar search. `#network-map` held a fixed `520px` min-height (`380px` on the narrowest breakpoint) regardless of viewport, leaving a large blank area below the map and sidebar on taller windows. | Remove the `lane-search` label/input and the `els.laneSearch` wiring (placeholder sync, three-way input sync, `clearFilters` reset); restore `#network-map`'s and `.lane-list`'s previous fixed `520px`/`min-height: 380px` values at all three breakpoints. | Explicit user decision, following a screenshot showing the blank map area and a request for a sidebar search box |
| LS-D037 | 2026-08-28 | The Network view's sidebar column is capped (`minmax(280px, 320px)` instead of `minmax(300px, .45fr)`) so it no longer keeps growing wider on large screens; the yellow "Truck" mode badge (`.lane-mode`) is removed from each Active trips/orders card, since `mapMode` is always `Truck` for this scope (VD2, one landmass) and repeating it on every card added no information. The top KPI cards (`.metric-grid`/`.metric-card`) are tightened: `min-height` 116 to 88px (mobile: 104 to 84px, 96 to 80px), padding 17x18 to 13x16px, icon 30 to 24px, value font 28 to 24px, tighter margins above the value and context lines, and the grid's own gap and bottom margin reduced. | The sidebar column scaled as a `.45fr` share of the row, so it grew very wide (and mostly empty) on wide screens; every card carried a yellow "TRUCK" badge; the KPI cards carried noticeably more padding and line spacing than their content needed. | Restore `grid-template-columns: minmax(0, 1.55fr) minmax(300px, .45fr)` on `.network-view.active`; restore the `.lane-mode` span in the `renderNetwork` card template and its CSS rule; restore the previous `.metric-grid`/`.metric-card` padding, min-height, icon size, value font size and margins at all three breakpoints. | Explicit user decision, following a screenshot of the Network view showing excess white space in both the sidebar cards and the KPI row |
| LS-D038 | 2026-08-28 | Selecting a trip/order on the Network map (marker click or lane-card click, `selectNetworkEntity`) moves that entity's card to the top of the Active trips/orders sidebar list and resets the list's scroll to the top, so the selection is visible without scrolling. `renderNetwork` reorders only the array it renders the sidebar cards from (`listEntities`); the underlying `filtered` array driving the map markers is untouched, so marker placement and the map's own selection/route logic are unaffected. `.lane-card.selected` is also made bolder: a 3px blue left border, a light blue (`--blue-soft`) card background, bold blue route text, and the "Open trip/order detail" button tinted to match. | The selected card kept its original position in the list (which could be off-screen, requiring a scroll to find), and its selected state was a thin blue border plus a faint shadow, easy to miss. | Remove the `listEntities` reorder block and the `list.scrollTop = 0` line in `renderNetwork`, reverting to rendering `filtered` directly; restore the previous single-line `.lane-card.selected` rule (border-color and box-shadow only) and remove the `.lane-card.selected strong`/`.lane-card.selected .lane-detail` rules. | Explicit user decision |
| LS-D039 | 2026-08-28 | `.table-wrap`'s height formula changes from `calc(100vh - 410px)` to `calc(100vh - 360px)` (the `min-height: 330px` floor is unchanged). The `410px` offset was calibrated against the taller header/KPI-card layout from before LS-D037 shrank the KPI cards; after that change the content above the table takes about 50px less space, so the old offset left a growing blank gap below the table as window height increased. Measured directly (headless browser, three window heights): the gap below the table is now a flat ~37px at every height, matching `.main`'s own intended bottom padding, instead of the roughly 90px dead strip the stale offset left behind. | `.table-wrap`'s `max-height` used a `410px` offset that no longer matched the actual header height after LS-D037, leaving a large blank strip below the table on tall windows. | Restore `max-height: calc(100vh - 410px)` on `.table-wrap`. Only correct to do this together with reverting LS-D037's KPI card sizing; reverting one without the other reintroduces a height mismatch in the other direction. | Explicit user decision ("please make the table fit the full size... my boss does not like a very blank space at the bottom") |
| LS-D040 | 2026-08-28 | The customer role (`state.role === "spvb"`, "Customer view") hides every exception-detail surface: the Exception column is dropped from the Orders table header and row cells (`renderTable`, `ORDER_COLUMNS` filtered by key rather than mutated, so `applySort`'s sort logic is unaffected); the "All exception states" filter (`els.exception`) and the "Orders With Open Exceptions" KPI card are hidden, with `.metric-grid` dropping to a 3-column layout (`.metric-grid.customer-view`) so no gap is left; a stale exception filter or metric selection is reset to "All"/"all" on switching to Customer view so it cannot keep silently filtering the list once its control is hidden; the map's red exception dot (`networkIcon`'s `fleet-alert` span) is suppressed; and the drawer's Exception history card is hidden (`openOrderDrawer`/`openTripDrawer`). All of this reverts automatically on switching back to Coordinator, since `setRole` now unconditionally calls `renderTable()` in both directions (previously only the switch-to-Customer direction forced a re-render, via `setPerspective("orders")`). Verified directly (headless browser): switching to Customer view removes the Exception header/cell, hides the filter and KPI card, hides the drawer's Exception history card, and produces a 3-equal-column KPI grid; switching back to Coordinator restores the Exception column (7 cells per row again), the filter, the KPI card and the 4-column grid. | The customer role only forced the Orders perspective and swapped the drawer's Journey milestone list (6 vs 9 milestones); every exception surface (table column, filter, KPI card, map marker dot, drawer history card) was equally visible to both roles. | Remove the `isSpvb`-conditional branches added to `renderTable` (column filter and `<td>` omission), `networkIcon` (alert suppression), `openOrderDrawer`/`openTripDrawer` (exception-history-card hidden toggle), and `setRole` (exception-filter/metric-card hidden toggles, `customer-view` class toggle, `state.exception`/`state.metric` reset, the trailing `renderTable()` call); remove the `.metric-grid.customer-view` CSS rule. | Explicit user decision ("customer should not see the exception") |
| LS-D041 | 2026-08-28 | The customer role also hides the Position source column (Observed/Computed/Manual badge, `renderTable`, same `ORDER_COLUMNS`-filter mechanism as LS-D040, now excluding both `"exception"` and `"source"` keys) and the "All sources" filter (`els.source`). Customer view's Orders table now shows 5 columns (Order, Trip, Ship-to, Route, ETA) instead of 7. `setRole`'s role-based hide/reset block moves to run after the `if (isSpvb) setPerspective("orders")` call rather than before it, because `setPerspective` unconditionally resets `els.source.style.display` itself (to show it outside the Exceptions perspective); hiding the source filter before that call was silently undone by it. `state.source` is reset to `"All"` alongside `state.exception` when switching to Customer view, for the same stale-filter reason as LS-D040. Verified directly (headless browser, both directions): Customer view shows 5 header columns with both filters hidden; switching back to Coordinator restores all 7 columns and both filters. | Position source (the Observed/Computed/Manual badge and its filter) was visible to the customer role, same gap class as LS-D040 covered for Exception. | Restore the `column.key !== "source"` clause removal in `renderTable`'s `orderColumns` filter and the `<td>` omission for the Position source cell; remove `els.source.style.display = isSpvb ? "none" : ""` and `state.source` reset from `setRole`; the reordering of `setRole`'s hide block relative to `setPerspective("orders")` can stay regardless, as it is a correctness fix independent of this feature. | Explicit user decision ("should not see position source also") |
| LS-D042 | 2026-08-28 | Three changes to the drawer's "Live journey map" legend and to the mock data generator. (1) All three legend entries drop their dash-pattern description, since the drawn line itself already shows it: "Observed · solid" to "Observed", "Estimated GPS gap · short dash" to "Estimated GPS gap", "Destination direction · long dash" to "Destination direction". (2) The customer role never sees the estimated-GPS-gap distinction: `routeDisplaySegments` forces `hasEstimatedGap` false whenever `state.role === "spvb"`, so the whole travelled path draws as plain Observed (solid green, `#15803d`) with no amber dashed segment (`#ef9d17`) and no "Last observed GPS" marker; the legend's "Estimated GPS gap" entry (`#legend-estimated-gap`) is hidden alongside it in `setRole`. This is the same internal-detail simplification as LS-D040/LS-D041, extended to the map. (3) The generated (non-hand-authored) order data now actually produces `Truck Queue in` as a `current` milestone: `generatedStageDetails["At Origin"]` (a flat `[current, next]` pair, always `["Truck Checked in", "Pick completed"]`) is replaced with `generatedAtOriginPairs`, two pairs alternating by `sequence % 2`: `["Truck Queue in", "Truck Checked in"]` and `["Truck Checked in", "Pick completed"]`. Before this, `Truck Queue in` existed only in `orderJourneyMilestones` (LS-D031) as a possible step on the drawer's journey stepper, but no generated order's `current` value ever actually landed on it, so the milestone was never exercised by the bulk of the mock dataset even though it is live for this Vietnam scope (VD5). Verified directly (headless browser): the legend now reads exactly `["Observed", "Estimated GPS gap", "Destination direction"]`; opening a Computed/In-Transit order's drawer as Coordinator draws an `#ef9d17` path and shows the legend entry, the same order in Customer view draws no `#ef9d17` path and hides the entry; at least one generated order (`SO-260825-1326`) now carries `current: "Truck Queue in"`. | The three legend entries repeated their dash pattern in words ("· solid", "· short dash", "· long dash"); the customer role's map showed the same observed/estimated distinction as the coordinator's; `generatedStageDetails["At Origin"]` was a single fixed pair, so no generated order ever showed `Truck Queue in` as its current milestone despite the milestone being live for this scope. | Restore the three legend spans' `" · solid"`/`" · short dash"`/`" · long dash"` suffixes; remove the `state.role !== "spvb" &&` clause from `routeDisplaySegments`'s `hasEstimatedGap` and the `document.getElementById("legend-estimated-gap").style.display` line from `setRole`; restore the flat `"At Origin": ["Truck Checked in", "Pick completed"]` entry in `generatedStageDetails`, remove `generatedAtOriginPairs`, and restore the plain `generatedStageDetails[stage]` destructuring (dropping the `stage === "At Origin" ? ... : ...` branch). | Explicit user decision ("Map of customer should also be different", "the dash is enough, remove the description of what dash it is", "Thêm milestone Queue In as it has changed to Vietnam") |
| LS-D043 | 2026-08-28 | The journey-stage filter chip that reads "Planning" in the Orders perspective (`configureJourneyStages`, `#stage-planning-label`) reads "Order received" instead when the role is Customer (`state.role === "spvb"`), matching the customer milestone vocabulary already established (`spvbJourneyMilestones` starts at "Order received"; every internal state before it, e.g. "truck queue in", already collapses to "Order received" via `toSpvbMilestone`). Only the displayed text changes; the chip's underlying filter value (`data-stage="Planning"`) is untouched, so `filteredEntities`' stage matching is unaffected by role. `setRole` now calls `configureJourneyStages(state.perspective === "orders")` unconditionally near its end (previously only the switch-to-Customer direction refreshed it, via `setPerspective("orders")`), so switching back to Coordinator while still in the Orders perspective does not leave the chip stuck reading "Order received". Verified directly (headless browser): Coordinator's Orders view reads "Planning", Customer view reads "Order received" with the same underlying `data-stage="Planning"`, and switching back to Coordinator restores "Planning". | The chip read "Planning" regardless of role, the coordinator's internal term rather than the customer-facing one used everywhere else in Customer view. | Remove the `isSpvb` conditional from `configureJourneyStages`'s `setText("stage-planning-label", ...)` call, restoring the plain `isOrders ? "Planning" : "Awaiting Dispatch"`; remove the trailing `configureJourneyStages(state.perspective === "orders")` call from `setRole`. | Explicit user decision ("in customer view, changed from planning to Order received") |
| LS-D044 | 2026-08-28 | Trip drawer's "Orders carried on this trip" card (`openTripDrawer`): the subtitle "Sales Orders and Stock Transfer Orders carried by the selected trip." is removed, and each order row becomes an expand-in-place accordion instead of an "Open order" button that navigated to the order's own drawer. The summary row (`.order-accordion-summary`, itself the whole `.relationship-item`) keeps the order number, ship-to (with the existing `legDestination`/`deliveredEnRoute` subtext, plus a new note when this order's ship-to differs from the trip's own primary `shipTo`), current-milestone badge, and a chevron; clicking it toggles a panel (`.order-accordion-panel`) showing a Ship-to line and SKU/product/case-quantity lines (`order.skuLines`). Every order now carries `skuLines`, 1 to 3 illustrative line items deterministically drawn from the four-SKU catalogue in `KB/CT_TCT_SPVB_Working_File_v11.md` V4 (Pepsi 390ml PET, 7Up 320ml can, Sting 330ml PET, Aquafina 500ml, all case of 24) with an invented case quantity per line; this is new illustrative data, not previously modelled anywhere in the mockup. This is related to, but distinct from, the still-open `LS-B007` (the same SKU/product/quantity/destination/ship-to accordion concept for the main compact Orders/Trips table row, not touched here). Verified directly (headless browser and screenshot): the subtitle is gone, clicking a row's chevron expands its panel with Ship-to and SKU lines without navigating away from the trip drawer (`drawerTitle` stays `TR-4127`, `drawerStillOpen` stays true). | Each order row showed a plain "Open order" button that opened that order's own drawer, leaving the trip drawer; the card's subtitle repeated what the heading already said; no order carried SKU, product or quantity data anywhere in the mockup. | Restore the `<p>Sales Orders and Stock Transfer Orders carried by the selected trip.</p>` line in the `#trip-orders-card` header; restore the previous `tripOrdersList.innerHTML` template (`<div class="relationship-item">` ending in a `<button data-open-order>` wired to `openOrderDrawer`) and remove the `.order-accordion-*` CSS rules; remove the `skuCatalog` constant and the `orders.forEach` block that assigns `order.skuLines`. | Explicit user decision ("Orders carried on this trip" screenshot: remove the subtitle, "Put SKU, product, destination, different ship to in accordion instead of opening order details") |
| LS-D045 | 2026-08-28 | The customer role's Orders table gains its own Status column, showing the customer-visible milestone (`toSpvbMilestone(entity.current)`, the same six-milestone vocabulary as the drawer's Journey card and the "Planning" to "Order received" chip of LS-D043), as a stage-coloured badge (blue In Transit, green At Destination, gray otherwise, the same convention already used on the trip-orders accordion and the trip-orders-card). This is new, not a restored column: the table has never shown milestone status directly, coordinator included (LS-D010 removed the old current/next milestone columns in favour of opening the drawer). `ORDER_COLUMNS` gains a `"status"` entry sorting by `spvbMilestoneRank` (the milestone's index along `spvbJourneyMilestones`, so it sorts by actual journey position, e.g. "Order received" before "In transit", not alphabetically) rather than `exceptionSortRank`-style urgency; `renderTable`'s column filter now excludes `"status"` for Coordinator (added alongside the existing exclusion of `"exception"`/`"source"` for Customer) so the column is exclusively rendered for the customer role, without touching Coordinator's column set. Verified directly (headless browser): Customer view's header reads Order, Trip, Ship-to, Route, ETA, Status (6 columns, `toSpvbMilestone` values visible as badges); clicking the Status header sorts "Order received" first; Coordinator's header is unchanged (Order, Trip, Ship-to, Route, ETA, Position source, Exception, 7 columns). | The customer role's Orders table had no visible indication of where an order stood in its journey at all, after LS-D040/LS-D041 removed Exception and Position source from it; only opening the drawer showed the Journey stepper. | Remove the `"status"` entry from `ORDER_COLUMNS`; remove the `spvbMilestoneRank` function; remove `column.key !== "status"` from the Coordinator branch of `renderTable`'s `orderColumns` filter, restoring the plain `ORDER_COLUMNS`; remove the `isSpvb` Status `<td>` line from the Orders row template. | Explicit user decision ("In the customer view, add the status which display the position of the milestone") |
| LS-D046 | 2026-08-28 | The Network view's Active trips/orders sidebar (`.lane-stats`) changes from `display: flex` to `display: grid; grid-template-columns: 88px 1fr; align-items: start;`. Under flex, each card's first cell ("X% travelled") sized to its own text, so its length ("4%" vs "66%" vs "100%") shifted the ETA time/date block sideways by a few pixels card to card; the fixed 88px first column now pins the ETA block to the same x position, top-aligned, on every card regardless of that text's length or whether the date wraps to a second line. | `.lane-stats`'s two cells ("X% travelled", the ETA time/date) sized to content under flex, so the ETA block's horizontal position varied slightly from card to card depending on the percentage text's length. | Restore `.lane-stats { display: flex; gap: 14px; margin-top: 8px; color: var(--ink); font-size: 11px; font-weight: 600; }`, dropping the grid-column and align-items rules. | Explicit user decision (screenshot of the ETA time/date block, "make this on the same position please, why one is on top while others is at the bottom") |
| LS-D047 | 2026-08-31 | `tileLayer()` now reads an optional CARTO API key from `localStorage.getItem("cartoApiKey")` and appends it to the basemap tile URL as `?key=...` when present, since CARTO now requires a key on this endpoint and unkeyed requests render a watermarked/degraded basemap. No key is hardcoded in this file: a personal key was shared over Slack for the team's use, but it is domain-scoped to whoever requested it and embedding any credential in this shared, git-tracked file is against project policy (`AGENTS.md`, "do not add credentials"). Each person or environment serving this file sets their own key once, in their own browser, via `localStorage.setItem("cartoApiKey", "YOUR_KEY")`; a key is free and issued instantly at https://carto.com/basemaps/apikey. | The tile URL carried no key, so CARTO rendered a degraded/watermarked basemap under their new key requirement. | Remove the `cartoKey`/`tileUrl` lines from `tileLayer()` and restore the static `"https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png"` URL string passed directly to `L.tileLayer`. | Team Slack message (CARTO basemap policy change, ATOM channel, 2026-08-31); key-handling approach is a project-policy application, not confirmed by the user |
| LS-D048 | 2026-08-31 | Supersedes LS-D047's "no key is hardcoded" stance. `tileLayer()` now carries `DEFAULT_CARTO_KEY`, the team key shared in the ATOM Slack channel, as a hardcoded fallback used whenever no per-browser `cartoApiKey` override is set in `localStorage`, so the basemap renders correctly out of the box with no per-viewer setup step. The `localStorage` override from LS-D047 is kept, so anyone can still substitute their own domain-scoped key without editing the file. | Per LS-D047: no key shipped in the file; the basemap only rendered correctly after a viewer manually ran `localStorage.setItem("cartoApiKey", ...)` in their own browser, which was the watermark the user's screenshot showed. | Remove the `DEFAULT_CARTO_KEY` constant and the `|| DEFAULT_CARTO_KEY` fallback in `tileLayer()`, restoring `cartoKey` to default to `""` as in LS-D047; this reopens the watermark for anyone without a `localStorage` override. Consider rotating or revoking the shared key at CARTO first, since it is now in git history. | Explicit user decision (key value given directly in chat, 2026-08-31); the key itself was shared for team-wide reuse in the ATOM Slack channel, 2026-08-31, by its requester |
| LS-D049 | 2026-08-31 | `renderOrderJourney` (the Journey panel's milestone track and time scale, used by both the Trip and Order drawers): the progress fill on both the step track (`.journey-progress-line`) and the time scale (`.journey-scale-progress`) now derives from `order.progress`/`trip.progress`, the same 0 to 100 figure that drives the map's travelled/remaining split, the "X of Y km" distance, and the network sidebar's "X% travelled" card (`LS-D046`). Previously the step track's fill was `currentIndex / (columnCount - 1)`, the fraction of the 8 evenly spaced steps marked done, unrelated to `progress`; a trip could sit at step 3 of 8 (43%) while its map position and distance read 60% along the route, which is the exact mismatch raised in the 28 August 2026 meeting ("your timeline is not at 60%"). Separately, time-scale ticks are spaced by real elapsed time (pre-transit milestones minutes apart crowd together, transit hours apart spreads out), so previously only the first, current and last ticks carried a visible `<b>` time label and the rest were tooltip-only (`title`/`aria-label`, hover-revealed), which read as "all your ticks are unpopulated" in the same meeting; a label now shows on any tick that clears a minimum pixel gap from the last shown one (estimated from the panel's own width, `34px` of headroom), always keeping the first, current and last regardless of crowding, so labels stay legible instead of stacking illegibly on the crowded side. The discrete step markers (done/current/issue/future per `.journey-step`) are unchanged, still keyed to `currentIndex`, so they can now visually sit ahead of or behind the continuous fill line; that is expected, since the two represent different things (last recorded milestone vs overall percentage complete). The `.journey-scale` panel also gains an inline `min-width` matching the step track's (`columnCount * 105px`), rather than the CSS default's flat 840px regardless of milestone count. | The step track's progress fill was the fraction of steps marked done, independent of `order.progress`; the time scale's fill was a separate ETA-derived fraction; only 3 of the (typically 6 to 9) ticks on the time scale showed a visible time label, the rest only on hover; the time scale's width did not scale with milestone count. | In `renderOrderJourney`, restore `progressWidth = currentIndex < 0 ? 0 : currentIndex / (columnCount - 1) * span`, restore `scaleProgressWidth = currentIndex < 0 ? 0 : Math.max(0, tickPositions[currentIndex] - 2)`, restore the `showLabel` conditional (`index === 0 \|\| index === currentIndex \|\| index === milestones.length - 1`) gating the `<b>` label in `scaleSteps`, and drop the `style="min-width:..."` on the `.journey-scale` div in the final template line. | `KB/Meeting/2026-08-26/Live Status Update - 2026_08_28.md` and its transcript; explicit user instruction, 2026-08-31, to fix the reported timeline mismatch and unpopulated ticks |
| LS-D050 | 2026-08-31 | The Exceptions perspective is removed: the "⚠ Exceptions" button from the Trips/Orders perspective switch, the `exceptions-perspective-button` click handler and its `?perspective=exceptions` URL parameter, `setPerspective`'s `isExceptions` branch (which repurposed the metric-grid tiles into exception counts and swapped in the exception table), the metric-card click handler's exceptions-perspective branch, `filteredExceptionEntries`, `renderExceptionsTable`, `updateExceptionMetrics`, `allExceptionEntries`, and `EXCEPTION_COLUMNS`. `renderTable`'s dispatch to it is gone, so the Trips/Orders table renderer is unconditional again. The per-row exception badge (`exceptionCell`, the "Exception" column on both tables) and the drawer's own Exception history card (`exceptionHistoryFor`/`renderExceptionHistory`) are unaffected and remain the only ways to see exception status from this mockup, since the dedicated Exceptions page (built and owned by the frontend team) is out of scope for this file. The "Trips/Orders With Open Exceptions" metric-grid tile (`data-metric="exceptions"`, a quick filter on the existing tables) is a separate feature and is also unaffected. | A third perspective, alongside Trips and Orders, showed a flattened one-row-per-exception aggregation table across every live order, with its own columns, filters and repurposed metric tiles. | Restore the `<button id="exceptions-perspective-button">` in the perspective switch, the `allExceptionEntries` and `EXCEPTION_COLUMNS` definitions, the `filteredExceptionEntries`/`renderExceptionsTable`/`updateExceptionMetrics` functions, the `isExceptions` branch and early return in `setPerspective`, the exceptions branch in the metric-card click handler, the `exceptions-perspective-button` click listener, and the `?perspective=exceptions` URL-parameter check; restore `if (state.perspective === "exceptions") { renderExceptionsTable(); return; }` at the top of `renderTable`. | `KB/Meeting/2026-08-26/Live Status Update - 2026_08_28.md` and its transcript ("we already have a full page for that... it's not needed here"); explicit user instruction, 2026-08-31 |
| LS-D051 | 2026-08-31 | A placeholder exception panel (`#exception-drawer`) is added: a blank panel showing only the words "Exception Handling" and a back arrow (`‹`, `#exception-drawer-back`). It reuses `.detail-drawer`'s own sizing and slide-in, at one z-index higher (1002 vs 1001), so opening it visually covers the open Trip or Order drawer without altering or closing that drawer underneath; going back (click, Escape, or the close-drawer button's own Escape/Tab handling, now scoped to whichever of the two drawers is open) removes only this panel, and the Trip/Order drawer reappears exactly as it was, per the meeting's aligned decision ("the exception side panel will overlap the active trip or order side panel, which will reappear if the exception panel is closed"). Triggered by a new "Open the exception" button added to every row in the drawer's own Exception history card (`renderExceptionHistory`), alongside the existing "Open order" button in the Trip drawer's context; both drawer's Escape/Tab handling (`closeDrawer`, the global keydown handler) now checks the exception panel first. The panel is deliberately blank: exception resolution is a page the frontend team owns and builds separately, out of scope for this mockup, so nothing beyond the interaction (stack, then go back) is modelled here. Not wired from the per-row exception badge in the main Trips/Orders table (`exceptionCell`) or from the small "Open the exception" preview card shown as a design reference; only from the drawer's own Exception history rows. | There was no exception-specific panel; the drawer's own Exception history rows showed only an "Open order" button (Trip context) or no action at all (Order context), and clicking neither opened anything resembling an exception view. | Remove the `#exception-drawer` markup and its CSS (`.exception-drawer`, `.exception-drawer-body`), the `exceptionDrawer` entry from `els`, the `openExceptionPanel`/`closeExceptionPanel` functions and their call sites (`closeDrawer`, the `exception-drawer-back` click listener, the Escape/Tab branches in the global keydown handler), and the `data-open-exception` button and its `.relationship-actions` wrapper from `renderExceptionHistory`. | `KB/Meeting/2026-08-26/Live Status Update - 2026_08_28.md` ("Aligned" decisions section); reference screenshots and explicit user instruction, 2026-08-31, to build it as a blank placeholder since another team owns the real page |
| LS-D052 | 2026-08-31 | The two YCH-operated HCM sites are renamed to match VD32 (`KB/CT_TCT_SPVB_Working_File_v11.md`): "MDC Hoc Mon" and its bare form "Hoc Mon" become "DOP"; "YCH DC Thuan An" and its bare form "Thuan An" become "OPMT". "Supply point Bac Ninh" becomes "Bac Ninh" (the "Supply point" prefix dropped), since VD32 reclassifies it from an invented supply point to one of YCH's three stated warehouses. Applied everywhere these strings appeared in the synthetic data, the multi-leg cases and the frozen worked example VN-TR-2214: `customer`/`origin`/`destination`/`route`/`currentPlace`/`originTime` fields, `generatedRouteTemplates`, `tripJourney` route strings, and the surrounding code comments (roughly 46 lines, all within the orders/multiLegCases data block, lines 989 to 1352; nothing in CSS, markup or rendering logic touched). No diacritics added (`Bac Ninh`, not `Bắc Ninh`), consistent with this mockup's existing plain-ASCII place names (Hai Phong, Quy Nhon, Nha Trang, Can Tho) and the deferred Vietnamese interface dictionary. The other five original supply points (Hai Phong, Hanoi, Quy Nhon, Nha Trang, Can Tho) are unchanged: VD32 does not reclassify them, and VD33 keeps the eleven-node network as the illustrative target state. The `channel: "SP"` field (a transfer-channel code, `KB/CT_TCT_SPVB_Working_File_v11.md` V2, "channel SP") and `generatedChannels`'s "SP" entry are also unchanged: that is a different concept (an internal transfer classification) from the retired "Supply Point" site-naming pattern the meeting objected to. | Site fields read "MDC Hoc Mon" and "YCH DC Thuan An" throughout (assumed names carried over from the previous, Indonesian scope, per V2's own "Assumed" tag), and "Supply point Bac Ninh" (an invented supply point, per V2's "Invented until stated" caveat, later superseded by VD32). | Revert with the inverse substitutions: "DOP" back to "MDC Hoc Mon" (or bare "Hoc Mon" where that was the original form), "OPMT" back to "YCH DC Thuan An" (or bare "Thuan An"), and "Bac Ninh" back to "Supply point Bac Ninh", across the same lines. | `KB/CT_TCT_SPVB_Working_File_v11.md` VD32; explicit user instruction, 2026-08-31 |
| LS-D053 | 2026-08-31 | Refines LS-D052: the channel badge (`.channel-badge`, next to a destination or ship-to name on the Orders and Trips tables) no longer prints the raw channel value "SP". A new `channelBadgeLabel(channel)` maps "SP" to "Transfer" for display only; "MT" and "GT" pass through unchanged. The underlying `channel` field, `generatedChannels`, and every other use of the value "SP" (filtering, search text, sorting) are unchanged, since the channel concept itself is not what VD32 or the meeting retired, only the on-screen word: a badge reading "SP" printed directly beside a place name (as in "Supply point Hai Phong" plus an "SP" pill) read as the same retired pattern regardless of the badge's actual, different meaning (a warehouse-to-warehouse transfer, `KB/CT_TCT_SPVB_Working_File_v11.md` V2, "channel SP"). | The badge printed the raw channel value, so a transfer showed literally as "SP", indistinguishable on screen from the retired "Supply Point" site-naming pattern. | Remove the `channelBadgeLabel` function and its two call sites (the two `class="channel-badge"` spans), reverting them to `${escapeHtml(entity.channel)}`. | User follow-up on LS-D052's screenshot ("why is the word SP still existing here"), 2026-08-31; user chose "rename the badge only" over leaving it or renaming the channel concept itself |
| LS-D054 | 2026-08-31 | Extends LS-D052 to the remaining five original supply points, which that entry deliberately left alone: "Supply point Hai Phong", "Supply point Hanoi", "Supply point Quy Nhon", "Supply point Nha Trang" and "Supply point Can Tho" all drop the "Supply point " prefix, leaving bare place names (`Hai Phong`, `Hanoi`, `Quy Nhon`, `Nha Trang`, `Can Tho`), in `customer`/`destination`/`legDestination`/`currentPlace` fields, `generatedRouteTemplates`, and code comments (23 occurrences, same orders/multiLegCases data block as LS-D052; nothing outside it touched). This goes beyond VD32's own table, which names only DOP, OPMT and Bac Ninh: flagged as a scope conflict rather than silently folded in. The node count and network structure are unchanged (still the eleven-node illustrative network VD33 keeps as the target state); only the "Supply point" wording is gone, matching the meeting's broader statement, not yet written into the working file as its own decision, that "we don't talk about SP anymore... there are only three destinations." | LS-D052 kept the "Supply point" prefix on these five, reasoning that VD32 only reclassifies DOP, OPMT and Bac Ninh and VD33 preserves the eleven-node network as-is. | Restore the "Supply point " prefix on the five affected place names across the same fields and lines. | User follow-up on LS-D052/LS-D053's screenshots ("why is the word Supply Point still here"), 2026-08-31; goes beyond what VD32's table itself states, so `KB/CT_TCT_SPVB_Working_File_v11.md` does not yet record this as a decision |
| LS-D055 | 2026-08-31 | Multi-drop delivery visualization, per the meeting's approved design ("show fractional progress... as partial opacity fill on the milestone marker, with hover/click for the breakdown", applying to "Arrived at ship-to" and "POD completed"). In `renderOrderJourney`, when the entity is a trip whose orders carry more than one distinct ship-to (`isMultiDrop`), the "Arrived at Ship-to" and "POD Completed" step nodes stop following the trip's single `currentIndex` and instead show, for each of those two milestones only, how many of the trip's orders have individually reached it: a filled circle at `rgba(49,95,137, fraction)` (opacity floor 0.22 so a small fraction stays visible), the fraction as visible text ("4 of 5") in place of the time, and the full breakdown by ship-to as both a `title` (hover) and a click-triggered popover (reusing `showExplanation`/`hideExplanation`, not the hidden `.explain-marker` system per LS-D019). Every earlier step (queue, pick, ex-warehouse, in transit) is unaffected, still one shared status for the whole trip, since a single truck's early journey is not yet split by drop. This reverses LS-D002 ("a trip carries one ship-to") for one new illustrative trip only, `TR-9401`, added as `multiDropCase` (5 orders, OPMT to Thu Duc/Di An/Bien Hoa/Long Thanh/Nhon Trach Distributors): 4 of 5 arrived, 2 of 5 POD completed, 2 carrying a "POD not received" exception, matching the meeting's own worked example. Every other trip stays single-drop; `sharedTripGroups` and the rest of LS-D002 are unchanged. | `renderOrderJourney`'s "Arrived at Ship-to" and "POD Completed" steps used the trip's single `currentIndex` like every other step, so a multi-drop trip's fractional delivery state had no visual representation; no trip in the data carried more than one distinct ship-to (LS-D002), so there was nothing to render a fraction against even if the code existed. | Remove the `dropOrders`/`isMultiDrop`/`dropsReaching` block and the `isMultiDrop` branch in the `steps` map (restoring the plain `done`/`current`/`issue`/`future` path for all 9 steps unconditionally), the click/keydown wiring for `.journey-node.multidrop`, the `.journey-node.multidrop { cursor: pointer; }` rule, and the `multiDropCase` array and its `orders.push(...multiDropCase)` call. | `KB/Meeting/2026-08-26/Live Status Update - 2026_08_28.md`; explicit user instruction, 2026-08-31, quoting the meeting's approved design; user chose to reverse LS-D002 for one demo trip after being flagged the conflict |
| LS-D056 | 2026-09-03 | `YCH_TCT_Live_Status_MVP_v0.24.html` replaces `YCH_TCT_Live_Status_MVP.html` as the active working file, so this pod's UI/UX aligns with the master mockup's own v0.24 chrome and design tokens (`UI/YCH_TCT_SPVB_v24.html`, its own `AGENTS.md`), per direction from the user relaying the boss's instruction to follow that file's UI/UX. All data and logic carry over unchanged; only the app shell changed: an icon-rail sidebar (`.v24-menu`, Live Status, Network, Trips, Dock operations, Decision threads, Playbook, Continuous improvement, Performance) replaces the plain workspace grid, the topbar gains live Hóc Môn/Quy Nhơn clocks, a language button and a persona picker (Tuấn L., Transport controller) and drops the in-header global search and role-switch, and the CSS custom properties (colors, radius, shadow) take the master's exact values. Two defects introduced by that re-skin, found and fixed before adoption: (1) the Coordinator/Customer view role-switch buttons (`#role-coordinator-button`, `#role-spvb-button`) were dropped from the header markup during the restructure while the CSS (`.role-switch-group`, moved-comment) and the `setRole`/init JS wiring (`getElementById`, `addEventListener`) still expected them, which would have thrown on page load and broken all script execution after that point; the buttons are restored in the page-header's `.header-meta` row, next to Snapshot/Refresh, matching where the carried-over CSS already expected them. (2) the file's own section-map comment was still the old file's, with stale line numbers and no mention of the new header/sidebar regions; it is rewritten to the file's actual current structure. Verified: a full `getElementById` id-reference scan against the DOM found zero missing ids (down from two before the fix); both `<script>` blocks pass `node --check`; `git diff --check` is clean. `YCH_TCT_Live_Status_MVP.html` is not deleted, per the project's own no-delete convention for superseded files; it stays as the pre-v0.24 historical record. | The active working file was `YCH_TCT_Live_Status_MVP.html`, styled with this pod's own original design tokens and a plain two-column workspace grid (sidebar removed per LS-D020), no icon-rail navigation, and the role-switch and global search living in the topbar. | To revert, resume editing `YCH_TCT_Live_Status_MVP.html` and treat `YCH_TCT_Live_Status_MVP_v0.24.html` as the frozen record instead; no data conversion is needed since the two files' underlying data and logic are identical at this entry. | Explicit user decision relaying the boss's instruction, 2026-09-03: "we need to follow the UI UX of the YCH_TCT_SPVB_v24.html... we will switch to v0.24.html from now on, not YCH_TCT_Live_Status_MVP.html anymore" |
| LS-D057 | 2026-09-03 | Four further alignments of `YCH_TCT_Live_Status_MVP_v0.24.html` onto the master's actual v0.24 chrome, all matched directly against `UI/YCH_TCT_SPVB_v24.html`. (1) Fonts: `--font-display`/`--font-body` were used throughout the ported header CSS (`.wordmark`, `.lang-btn`, `.persona-pick`, `h1`-`h4`, `.metric-value`, `.badge`) but never defined in `:root`, so those rules silently fell back to the inherited body font instead of Plus Jakarta Sans; both variables are added with the master's exact fallback stacks, and `body`'s font-family now reads `var(--font-body)` instead of a separately hardcoded value. (2) Stage-chip filters (`.stage-chip`, the Journey-stage row): border-radius changed from an 18px pill to `var(--radius)` (6px), background/text colors switched to the shared tokens, and `font-family: var(--font-display)` added, matching the master's `.fchip` box styling exactly (`--slate`/`--line`/`--grey-100` in the master equal `--gunmetal`/`--border`/`--surface-soft` here). (3) Lana, the SIMCEL AI copilot: a `.lanabtn` topbar button and a slide-in `.lana` panel are added, styled to match the master's `.lanabtn`/`.lana` CSS (colors mapped the same way as the chips); the avatar uses a plain initial-in-a-circle rather than the master's embedded photo, and the panel's body is a static preview (intro text and three suggested questions) with no live model behind it, since no such backend exists for this pod. (4) Persona picker: the topbar's static, single-identity `.persona-pick` (hardcoded "Tuấn L., Transport controller") is replaced with the master's actual mechanism, a `PERSONAS` object and a working `pmenu` dropdown (`buildPMenu`, `setPersona`, `togglePMenu`), carrying the master's own three personas verbatim (Tuấn L., Control tower coordinator; Hòa N., Warehouse supervisor, DOP; Khánh P., Transport supervisor) plus a fourth this pod adds, Mai H., Logistics coordinator, SPVB, an invented Vietnamese name standing in for the customer. Selecting any of the first three calls `setRole("coordinator")`; selecting Mai H. calls `setRole("spvb")`, so she sees exactly the customer view already defined by LS-D040 to LS-D045 (Orders perspective only, no Exception or Position source columns, no exception detail on the map or in the drawer, "Order received" milestone wording). The separate Coordinator/Customer view button pair added in LS-D056 is removed as redundant now that the persona picker is the single, correct trigger: `setRole` no longer references those two button ids, and its old `getElementById`/`addEventListener` calls on them are removed rather than left to throw again. Verified: a full `getElementById` id-reference scan found zero missing ids; both `<script>` blocks pass `node --check`; `git diff --check` is clean. | Fonts: `--font-display` was undefined, so headings, the wordmark, the persona name and the lang button silently rendered in the body font. Stage chips: an 18px pill radius and no explicit font-family, unrelated to the master's boxed `.fchip` look. No Lana button or panel existed. The persona picker showed one fixed identity with no dropdown and no customer-facing persona; the only way to reach Customer view was the LS-D056 button pair in the page header. | To revert: remove the two font variables and restore body's hardcoded font-family; restore `.stage-chip`'s 18px border-radius and prior colors; remove the `.lanabtn`/`.lana` markup, CSS and JS (`toggleLana`, the `#lana`/`#lana-button`/`#lana-close` wiring); remove `PERSONAS`, `buildPMenu`, `setPersona`, `togglePMenu`, the `#pmenu` markup and CSS, and restore the LS-D056 role-switch button pair in the page-header along with `setRole`'s and the init block's references to `role-coordinator-button`/`role-spvb-button`. | `UI/YCH_TCT_SPVB_v24.html` (master mockup, read directly per its own `AGENTS.md`); explicit user instruction with screenshots, 2026-09-03, quoting the master's font, chip, Lana and persona-picker appearance and asking for a Pepsico/customer persona "look at the decision log to see for this one" (LS-D040 to LS-D045) |
| LS-D056 | 2026-09-03 | CSS design tokens updated to match the v0.24 master mockup (`YCH_TCT_SPVB_v24.html`): `--canvas` changed from `#eef2f4` to `#F6F8F9`; `--shadow` changed from a heavier multi-layer value to `0 1px 2px rgba(36,51,63,.06)`; `--shadow-pop` changed to `0 8px 24px rgba(32,32,32,.10), 0 2px 6px rgba(32,32,32,.06)`; `--radius` changed from `8px` to `6px`; `--radius-l` added as `8px`; `--border` changed from `var(--border-strong)` references to `#E4E8EB`; `--blue` changed from `#2563EB` to `#2E5C86` (the v0.24 slate-blue). | `--canvas: #eef2f4`, heavier multi-layer shadow, `--radius: 8px`, blue `#2563EB`. | Revert the six token values in `:root` to their previous values listed here. | Boss instruction, 2026-09-03: "ATOM TCT SPVB is now on v0.24"; v0.24 master mockup used as reference. |
| LS-D057 | 2026-09-03 | `.metric-card` updated to v0.24 appearance: `border-radius` set to `var(--radius)` (6px, down from 10px); `background` changed from `white` to `var(--surface)`; `box-shadow` changed from a heavy multi-layer to `var(--shadow)`. | `border-radius: 10px`, `background: white`, heavy shadow. | Restore previous `.metric-card` border-radius, background and box-shadow values. | v0.24 master mockup reference. |
| LS-D058 | 2026-09-03 | `.panel` updated to v0.24 appearance: `border-radius` set to `var(--radius-l)` (8px); `background` changed to `var(--surface)`; `box-shadow` changed to `var(--shadow)`. | Heavier radius, previous shadow and background values. | Restore previous `.panel` border-radius, background and box-shadow values. | v0.24 master mockup reference. |
| LS-D059 | 2026-09-03 | Table header `th` updated to v0.24 style: `text-transform: uppercase`, `letter-spacing: .5px`, `font-size: 9.5px`, `background: var(--canvas)`. Previously `th` had no text-transform, default letter-spacing and a `#F2F5F7` background. | No `text-transform`, no `letter-spacing`, `background: #F2F5F7`. | Remove the three new properties from `th` and restore `background: #F2F5F7`. | v0.24 master mockup reference. |
| LS-D060 | 2026-09-03 | Table cell `td` updated: `border-bottom` changed from `var(--border-strong)` to `var(--border)`; body text color changed from `var(--ink)` to `var(--gunmetal)`; `font-size` set to `12.5px`. | `border-bottom: 1px solid var(--border-strong)`, default body text color, no explicit font-size on `td`. | Restore the three `td` values to their previous declarations. | v0.24 master mockup reference. |
| LS-D061 | 2026-09-03 | Status badge colors updated to v0.24 pill palette: `.badge.green` becomes `background:#E8F5E9; color:#2E7D32`; `.badge.amber` becomes `background:#FFF8E1; color:#F57F17`; `.badge.red` becomes `background:#FBECEA; color:#C0392B`; `.badge.blue` becomes `background:#EAF0F6; color:#2E5C86`. | Previous badge colors used different hex values for each state. | Restore the four previous badge `background` and `color` values. | v0.24 master mockup reference. |
| LS-D062 | 2026-09-03 | `.metric-value` typography updated to v0.24: `font-size: 27px` (from 32px); `letter-spacing: -.5px` added; `font-family: "Plus Jakarta Sans", sans-serif` added. | `font-size: 32px`, no `letter-spacing`, no explicit `font-family` on `.metric-value`. | Remove `letter-spacing` and `font-family`, restore `font-size: 32px`. | v0.24 master mockup reference. |
| LS-D063 | 2026-09-03 | App shell layout changed from a simple `min-height: 100vh` wrapper to a CSS grid (`grid-template-columns: var(--railw) 1fr; grid-template-rows: 56px 1fr; grid-template-areas: "header header" "menu main"`). `--railw: 50px` added as a CSS variable. The `.workspace` div is now `grid-area: main; overflow-y: auto`. | `.app-shell { min-height: 100vh; }` with no grid; `.workspace` used `display: grid; grid-template-columns: minmax(0,1fr); min-height: calc(100vh - 56px)`. | Remove the grid declarations from `.app-shell`, remove `--railw`, restore `.workspace` to the previous rules. | v0.24 master mockup grid structure (`YCH_TCT_SPVB_v24.html` lines 228-229). |
| LS-D064 | 2026-09-03 | Topbar header replaced with the v0.24 dark header style. The old `<header class="topbar">` (YCH brand-mark text block, global search bar, role-switch, notification icon, avatar initials) is replaced by `<header class="app-header">` containing: the ATOM TCT SVG glyph and wordmark with gamboge "TCT" accent; a `.topbar` flex bar with a vertical divider, live ICT clocks for Hoc Mon and Quy Nhon, an EN language button, and a persona picker ("Tuấn L. / Transport controller"). The role-switch remains in the page-header section. A live clock (`setInterval`, ICT via `Intl.DateTimeFormat`) is added. | `<header class="topbar">` with YCH text brand-mark, global search input, role-switch, notification button and avatar initials "FA". | Restore the old `<header class="topbar">` markup and its CSS rules; remove `.app-header`, `.brand .glyph/.btext/.wordmark/.context`, `.topbar .clock`, `.lang-btn`, `.persona-pick`, and the `updateClock` function. | Boss instruction, 2026-09-03 (v0.24 announcement); v0.24 master mockup header structure. |
| LS-D065 | 2026-09-03 | v0.24 icon rail sidebar (`.v24-menu`) added. A `<div class="v24-menu">` is inserted between the app header and the workspace, occupying `grid-area: menu`. It contains 8 icon-only navigation items (Work/Live Status active, Network, Trips, Dock operations, Decision threads, Playbook, Continuous improvement, Performance) as SVG icons sourced directly from `YCH_TCT_SPVB_v24.html` lines 1822-1846, a numeric badge (9) on the active item, and a "v0.24" build tag at the foot. Hover tooltips show the page name via CSS `::after` on `[data-tip]`. The old `.sidebar`/`.nav-item` text-based nav CSS is retained but no longer rendered (no matching HTML). | No sidebar HTML element existed (`.sidebar` CSS was present but unrendered). | Remove the `<div class="v24-menu">` markup and its CSS block (`.v24-menu`, `.v24-menu a`, `.v24-menu .nav-ico`, `.v24-menu .nav-badge`, `.v24-menu .build-tag`, the `[data-tip]::after` tooltip rule). | Boss instruction, 2026-09-03 (v0.24 announcement); v0.24 master mockup nav structure, lines 1822-1847. |
| LS-D066 | 2026-09-03 | Google Fonts CDN link added to `<head>` for Plus Jakarta Sans (weights 400, 500, 600, 700, 800) and DM Sans (weights 400, 500, 600, 700, italic), replacing the previous `@import url(...)` inside the `<style>` block. The `<link>` approach loads earlier and avoids the import round-trip inside CSS. Both families are already referenced in the file's CSS variables (`--font-display`, `--font-body`) and throughout the component CSS, but were not loading reliably from the `@import` alone in some browsers opening the file locally. | Font families declared in CSS variables but loaded only via `@import url('https://fonts.googleapis.com/...')` inside `<style>`; locally-opened files sometimes blocked this before the style block applied. | Remove the three `<link rel="preconnect">` and `<link rel="stylesheet">` tags added for Google Fonts; restore reliance on the `@import` inside `<style>` if needed. | User observation, 2026-09-03 ("why is the text font so different"); v0.24 master mockup uses embedded woff2 fonts; this file uses CDN as the equivalent. |
| LS-D067 | 2026-09-03 | A hidden `<input id="global-search">` (zero size, `opacity:0`, `pointer-events:none`) is added immediately before `.app-shell`. When LS-D064 removed the visible global search bar from the header, `els.globalSearch` in the JS became `null`, causing a `TypeError` before `setPerspective("trips")` was called, which prevented the entire table and metric data from rendering (all counts showed 0, table empty). The hidden input restores the DOM element the existing event-wiring expects without surfacing a second search box to users. | `els.globalSearch` was `document.getElementById("global-search")` pointing to the visible search input in the old header; after LS-D064 removed that header the element was absent, crashing the initialization loop. | Remove the hidden input and either restore the visible `id="global-search"` input in the header (reverting LS-D064 partially) or null-guard every `els.globalSearch` reference in the script. | Self-identified JS crash, 2026-09-03, after LS-D064 was applied and user reported all data showing as 0. |
| LS-D068 | 2026-09-03 | Live clock JavaScript moved into a properly closed `<script>` tag as a self-invoking function (IIFE with recursive `setTimeout`). In a prior session the clock update code was appended after the main `</script>` closing tag, placing raw JavaScript text in the HTML body where it rendered as visible plain text instead of executing; as a result the ICT clock displays in the header never updated from "00:00". The clock code is now a separate `<script>` block at the end of `<body>` with its own open and close tags, using `(function updateClock() { ... setTimeout(updateClock, 10000); })();` so it starts immediately on DOMContentLoaded and refreshes every 10 seconds. | Clock IIFE code was appended as raw text after the main script's `</script>`, visible in the rendered HTML body but not executed by the browser. | Remove the separate clock `<script>` block added at the end of `<body>`; if the clock code is needed, it must be placed inside a properly opened `<script>` tag. | Self-identified parse error, 2026-09-03; user reported data appearing only after switching perspectives, which was one symptom of the initialization cascade failing. |
| LS-D069 | 2026-09-03 | `setPerspective("trips")` and all URL-parameter initialization calls wrapped in `window.setTimeout(function() { ... }, 0)` to defer them one event-loop tick after the synchronous script block completes. Previously the call ran synchronously at parse time, before the browser had committed the final CSS layout and grid dimensions; on some runs this caused `renderTable` to complete before the DOM was in a stable state, producing correct data counts from the JS perspective but blank or zero-reading metric tiles and an empty table visually until a user interaction triggered a re-render. With the 0ms defer the entire initialization block runs after layout is settled, making the first render reliable without requiring any user interaction. | `setPerspective("trips")` was called synchronously as the last statement of the main `<script>` block; the URL-parameter checks for perspective/role/view/fleet/order/trip immediately followed. | Remove the `window.setTimeout(function() {` wrapper and its `}, 0);` closing line, restoring synchronous calls to `setPerspective("trips")` and the URL-parameter block directly at the bottom of the main script. | User report, 2026-09-03: "The data are lost when we are in trips view. Then I click to orders view and it appears, then click again trips view and data are shown again"; deferred init is the standard browser fix for first-render timing races. |
| LS-D070 | 2026-09-04 | Supersedes the avatar clause of LS-D057 ("the avatar uses a plain initial-in-a-circle rather than the master's embedded photo"). All three Lana avatar spots (`.lanabtn` topbar button, `.lana .lhead .lava` panel header, `.lana .lintro .lava-lg` panel intro) now show the master mockup's actual embedded avatar photo (`<img alt="Lana">`, a base64 PNG data URI, identical payload copied verbatim from all three of its own occurrences in `05. Product/00. Global/Mockup/Released/YCH_TCT_SPVB_v24.html`) instead of a plain "L" initial. `.lanabtn img`, `.lana .lhead .lava img` and `.lana .lintro .lava-lg img` gain `width:100%;height:100%;object-fit:cover;display:block;background:#fff`, matching the master's own image-fit rules; `.lava` and `.lava-lg` gain `overflow:hidden` so the image is clipped to the circle; the now-unused `.lana-av` text-styling rule (font-family/size/weight/color) is removed and the font/color/background text-avatar properties are dropped from `.lava`/`.lava-lg`, since they no longer hold text. | The three avatar spots each rendered a colored circle (`var(--violet)` background) containing the letter "L" in Plus Jakarta Sans bold, via `<span class="lana-av">L</span>`, `<div class="lava">L</div>` and `<div class="lava-lg">L</div>`. | Replace the three `<img src="data:image/png;base64,..." alt="Lana"/>` elements with `<span class="lana-av">L</span>`, `<div class="lava">L</div>` and `<div class="lava-lg">L</div>` respectively; restore the `.lana-av` rule (`font-family: var(--font-display); font-size: 13px; font-weight: 700; color: #fff;`) in place of `.lanabtn img`; restore the single-line `.lana .lhead .lava` and `.lana .lintro .lava-lg` rules with their prior `font-family`/`font-weight`/`font-size`/`color` text-avatar properties and drop `overflow: hidden` and the two `... img` rules. | Explicit user instruction, 2026-09-04, to take the Lana AI avatar from `YCH_TCT_SPVB_v24.html` and fill it into `YCH_TCT_Live_Status_MVP_v0.24.html`, which had the Lana panel but was missing the avatar image |
| LS-D071 | 2026-09-04 | The `.v24-menu` icon-rail sidebar's `active` highlight (light-blue background, `.v24-menu a.active`) moves from the "Live Status" item (top, grid icon, carries the `nav-badge` "9") to the "Trips" item (third, truck icon). Only the `class="active"` attribute moves between the two `<a>` tags; the `nav-badge` "9" stays on the Live Status item, and no other markup, routing or page content changes, since every rail item is a static, non-navigating placeholder link in this single-page mockup. | The `active` class, and its light-blue highlight, sat on the "Live Status" item. | Move `class="active"` back from the "Trips" `<a>` to the "Live Status" `<a>`. | Explicit user instruction, 2026-09-04, with a screenshot of the icon rail asking to move the highlighted page to "Truck" instead of the first page |
| LS-D072 | 2026-09-04 | Two changes to the Trips table, from a screenshot showing excess white space. (1) The "Orders carried" column's cell drops its `subtext` line listing up to three order IDs (`entity.orderIds.slice(0, 3).join(" · ")`, plus a "+N" overflow suffix); the `entity-count` badge ("N orders") is now the cell's only content. The underlying `orderIds` field, and its use in the global search's matched text, are unchanged; only this display line is removed. (2) The shared `td` rule changes from a fixed `height: 52px; padding: 0 12px` to `padding: 8px 12px` with no explicit height, so each table row's height now follows its tallest cell's actual content instead of a flat 52px for every row regardless of content. This shrinks single-line cells (e.g. the plain-text Route column, which had no subtext line and so sat mostly empty inside the fixed 52px) and any row whose cells are now all single-line after change (1), while multi-line cells (Trip's route subtext, ETA's context line, Position source's freshness subtext, Exception's subtext) still get the room they need since row height is content-driven, not fixed. Both the Trips and Orders tables share the one `td` rule, so the same tightened spacing applies to both. | The "Orders carried" cell showed up to three order IDs beneath the count badge. Every table row was a fixed 52px tall regardless of whether its cells held one line of text or two, leaving visibly empty vertical space in single-line cells like Route. | Restore the `<span class="subtext">...</span>` order-ID list in the Orders-carried `<td>` (`${escapeHtml(entity.orderIds.slice(0, 3).join(" · "))}${entity.orderCount > 3 ? \` · +${entity.orderCount - 3}\` : ""}`); restore `td { height: 52px; padding: 0 12px; ... }`. | Explicit user instruction, 2026-09-04, with screenshots of the Orders-carried column and the Route column asking to drop the order-ID list and reduce table white space |
| LS-D073 | 2026-09-04 | Refines LS-D072: the Trips/Orders table's horizontal column widths are now explicit and proportioned, instead of the browser's default auto-sizing leaving the Trip and Route columns very wide with dead space while Orders-carried and Ship-to sat cramped (per a follow-up screenshot after LS-D072's row-height fix). `table` gains `table-layout: fixed`; `th, td` gain `overflow: hidden` so any content exceeding its assigned width clips rather than bleeding into the next column. Each `ORDER_COLUMNS`/`TRIP_COLUMNS` entry gains a `width` field (Order 130px, Trip 110px, Orders carried 140px, Ship-to 190px, Route 190px, ETA 90px, Position source 150px, Exception 220px, Status 130px), and `sortableHeaderCell` now emits it as an inline `style="width:..."` on the `<th>`; since the table is `width:100%` and fixed-layout distributes any surplus proportionally across all specified column widths, the assigned px values act as relative ratios rather than hard caps, so columns still stretch to fill the table but in far better proportion to their actual content than unconstrained auto-layout gave them. Verified directly (headless browser, three screenshots: Trips view, Orders view, and the SPVB/customer 6-column Status view): Trip-to-Orders-carried and Route-to-ETA gaps are visibly reduced, no header label wraps or clips at its assigned width, and multi-line subtext (e.g. "Earlier trip on this order's journey") still renders in full within its column. | `table` had no `table-layout` (default `auto`); `<th>`/`<td>` carried no explicit width; column widths were computed by the browser from content plus a `white-space:nowrap` table-wide, which gave the Trip and Route columns much more width than their short content needed while Orders-carried and Ship-to were comparatively cramped. | Remove `table-layout: fixed` from `table` and the `th, td { overflow: hidden; }` rule; remove the `width` field from every `ORDER_COLUMNS`/`TRIP_COLUMNS` entry; remove the `column.width ? \` style="width:${column.width}"\` : ""` conditional from `sortableHeaderCell`, restoring the plain `<th scope="col">`. | Explicit user instruction, 2026-09-04, with a screenshot of the Trips table calling out the gap between the Trip and Orders-carried columns and asking to balance column spacing to optimize space |
| LS-D074 | 2026-09-04 | LS-D055's illustrative multi-drop trip is renamed for the meeting demo raised in `Meeting/2026-08-26/Live Status Update - 2026_08_28.md`'s "Needs Further Discussion"/"Aligned" split: its trip `Trip-9401` becomes `Trip-A`, and its five orders `SO-260826-2501` to `SO-260826-2505` become `SO-A1` to `SO-A5`, numbered by drop order (A1 is drop 1 of 5 Thu Duc, ... A5 is drop 5 of 5 Nhon Trach, the in-transit leg), not by their position in the `multiDropCase` array. Only the `order`/`trip` id strings and the surrounding code comments change; every other field (customer, route, ETA, exception, coordinates, `skuLines` if any) is untouched. Separately, `orders.push(...multiDropCase)` becomes `orders.unshift(...multiDropCase)`, so Trip-A's five orders sit at the front of the `orders` array rather than the back; since `buildTrips` groups by first-seen order per trip (Map insertion order) and the default `state.sortKey` is `null` (no sort applied until a user clicks a column header), this puts Trip-A first in `liveTrips` and its five SO-A orders first in `liveOrders` on initial page load, before any filtering, search or column sort - visible at the top of the Trips table, the Orders table, and the customer/SPVB Orders table (same underlying array, just a different column set) without any dedicated pinning logic. Verified directly (headless browser, four screenshots): Trip-A is the first row in the default Trips view; SO-A5, SO-A1, SO-A2, SO-A3, SO-A4 are the first five rows in both the coordinator Orders view and the SPVB Orders view (in that order, since SO-A5's in-transit entry is deliberately first in `multiDropCase` so `buildTrips` derives Trip-A's own summary fields from it); `?trip=Trip-A` still deep-links to the trip's drawer. | The trip was `Trip-9401` and its five orders were `SO-260826-2501` to `SO-260826-2505`, date-coded like the rest of the generated dataset and easy to lose track of by sight. `orders.push(...multiDropCase)` placed the five orders at the end of the `orders` array, so Trip-A/its orders rendered wherever normal array/grouping order put them (not necessarily near the top) on every table. | Revert the six id strings (`Trip-A` to `Trip-9401`, `SO-A1`..`SO-A5` to `SO-260826-2501`..`SO-260826-2505`, matched to the same drop-order mapping) across `multiDropCase` and its comments; change `orders.unshift(...multiDropCase)` back to `orders.push(...multiDropCase)`. | Explicit user instruction, 2026-09-04, resolving the multi-drop meeting item first: "make the multi-drop id as Trip-A... SO-A1, SO-A2, STO-A1... put it at the top for every page, including trips, orders and Pepsico view" |
| LS-D075 | 2026-09-04 | First mockup answer to the meeting's other open item ("Exception classification structure requires further discussion... whether they should be separated or inherited" between orders and trips): the trip drawer's Exception history card now renders two explicitly separate groups, "Trip exceptions" and "Order exceptions", instead of one flat blended list. A new `tripExceptionHistory` field holds exceptions owned by the trip itself (authored on a trip's first order only, e.g. Trip-A's, and carried into the grouped trip object by `buildTrips`); it is rendered by a new `exceptionGroupHtml` helper (shared with the existing per-order aggregation, both built on a new `exceptionItemHtml` helper factored out of the old single template) as the first group, with the existing per-order `exceptionHistory` aggregation as the second. The order drawer is unaffected: a single order has no trip-owned exceptions of its own, so `renderExceptionHistory(entity, true)` still renders one flat list. This is a UI demonstration of one possible classification (trip-owned vs order-owned, kept apart, not inherited into each other), not a decision on the meeting's actual question, which still needs the room's answer. Separately, and requested alongside it: (1) Trip-A gains an illustrative trip-owned exception, "Route deviation" (PB-19, the in-transit catalogue entry per `KB/CT_TCT_SPVB_Working_File_v14.md` V9), status Resolved, with a note describing a detour around a closed road section; this is the first exception anywhere in the mockup authored on `tripExceptionHistory` rather than an order's `exceptionHistory`. (2) The "Orders carried on this trip" accordion summary row (in `openTripDrawer`) now shows each order's exception status badge (via `latestExceptionStatus`) next to its milestone badge, wrapped in a new `.order-status-group` flex row, so an order's open exception is visible without expanding its row; previously the milestone badge was the row's only status signal. Verified directly (headless browser, three screenshots at a realistic 1400px width): Trip-A's card shows "TRIP EXCEPTIONS 1" (Route deviation, Resolved) above "ORDER EXCEPTIONS 2" (both POD-not-received entries, tagged SO-A3/SO-A4); SO-A3 and SO-A4's accordion rows show "Arrived at Ship-to" plus an "Open" badge side by side; the order drawer for SO-A3 still renders its one-entry flat list unchanged. | The trip drawer's Exception history card listed every carried order's exceptions in one undifferentiated list, with no way to tell a trip-owned issue (there were none) from an order-owned one. The "Orders carried on this trip" row showed only the milestone badge; an order's exception was invisible until its row was expanded. Trip-A had no trip-owned exception at all. | Remove `tripExceptionHistory` from Trip-A's first order and from `buildTrips`'s grouped-object construction; remove `exceptionItemHtml` and `exceptionGroupHtml`, restoring `renderExceptionHistory`'s single-list body (`entries.map(...).join("")` on the combined `exceptionHistoryFor`-per-order aggregation only, no trip group); remove the `orderExceptionStatus`/`orderExceptionBadge` lines and the `.order-status-group` wrapper from the accordion row template in `openTripDrawer`, restoring the plain single milestone badge; remove the `.exception-group`/`.exception-subhead`/`.order-status-group` CSS rules. | Explicit user instruction, 2026-09-04, on a screenshot of Trip-A's drawer: "adding an exception status right next on the orders carried on this trip, next to the status milestone... the exception history should view all exception for trip and order separately: can you add an exception where the truck deviate from the route and the status is resolved" |
| LS-D076 | 2026-09-04 | Supersedes LS-D075's "Trip exceptions"/"Order exceptions" split, on user follow-up ("Dont seperate the trip and order exception, i still not decided yet"): the trip drawer's Exception history card is one flat list again, not two groups. `exceptionGroupHtml` and `.exception-group`/`.exception-subhead` CSS are removed. Two refinements to that single list, also requested: (1) entries sort Open first, then Snoozed, then Resolved (`entryStatusWeight`, a new small comparator, replacing array order), so what needs attention surfaces above what is already handled; (2) `exceptionItemHtml` now omits the "Open the exception" button entirely unless `entry.status === "Open"` (a Snoozed or Resolved entry keeps "Open order" when it has one, but drops the action button, since there is nothing left to action). Trip-A's "Route deviation" (added in LS-D075) still renders as a single entry, not duplicated per carried order, since `tripExceptionHistory` is retained unchanged as a mechanism; only the grouped *display* is reverted, per the user's explicit note to remember this distinction for future trip-owned exceptions ("if it is related to the truck/trip, it should appear... instead of repeatedly duplicating for each order/stock transfer order inside the trip"). Verified directly (headless browser, 1400px width, `?trip=Trip-A`): the list reads Open (SO-A3 POD not received), Open (SO-A4 POD not received), Resolved (Route deviation, no action button) top to bottom, one list, no subheadings. | LS-D075's two-group split (Trip exceptions above Order exceptions) and array-order-only entries (no status sort), with "Open the exception" always shown regardless of status. | Restore `exceptionGroupHtml` and the `.exception-group`/`.exception-subhead` CSS rules; restore `renderExceptionHistory`'s two-call body (`exceptionGroupHtml("Trip exceptions", ...) + exceptionGroupHtml("Order exceptions", ...)`) in place of the single sorted `entries` array; remove `entryStatusWeight` and the `.sort(...)` call; restore the unconditional `<button type="button" data-open-exception>Open the exception</button>` in `exceptionItemHtml`. | Explicit user instruction, 2026-09-04: "Exception with open status should be on the top, the snoozed and the resolved should be put at the bottom and should not have the Open The Exception itself. Dont seperate the trip and order exception, i still not decided yet. Remember this one, if it is related to the truck/trip, it should appears something like this instead of repeatedly duplicating..." |
| LS-D077 | 2026-09-04 | "Orders carried on this trip" gains a labelled, equal-width four-column layout: a new static header row (`.order-carried-columns`, "SO/STO", "Ship-to", "Milestone", "Exception status") sits above the list, and both it and every accordion row (`#trip-orders-list .relationship-item`) share `grid-template-columns: minmax(0,1fr) minmax(0,1fr) minmax(0,1fr) minmax(0,1fr) auto` so the four labels line up exactly with their data cells. This required splitting what LS-D075 had combined into one flex-wrapped cell (`.order-status-group`, milestone badge + exception badge together) back into two separate grid cells, one per column, dropping the now-unnecessary `.order-status-group` wrapper; an order with no exception now explicitly reads "None" (a `<span class="none">`) in the Exception status cell rather than being blank. `minmax(0, ...)` (not bare `1fr`) is required on every track: a bare `1fr` track's default `min-width:auto` sizes to its content's min-content width, which measured wider than the available space here and silently pushed the row's later columns off past the drawer's own edge with no visual warning (confirmed by an `overflow:hidden` on `.detail-drawer` clipping it, and by direct `getBoundingClientRect()` measurement before and after adding `minmax(0, ...)`); this scoped, `#trip-orders-list`-only rule leaves the shared `.relationship-item` default (used by the Exception history rows, a different column shape) untouched. Verified directly (headless browser, 1400px width, `virtual-time-budget=4000` to let layout settle before capture, plus a `getBoundingClientRect()` probe on every header and row cell): all four header labels and all five data columns (including the chevron) render fully inside the card with no clipping, each of the four data columns measuring an identical ~258.5px header / ~255px row width. | The row's milestone and exception badges shared one flex-wrapped grid cell (`.order-status-group`, LS-D075) with no column headers or labels at all; the list's implicit column widths (`minmax(150px,.6fr) minmax(180px,1fr) minmax(150px,.8fr) auto`, the shared `.relationship-item` default) were uneven and unlabelled. | Remove the `.order-carried-columns` header row markup and its CSS rule; remove the `#trip-orders-list .relationship-item` grid-template-columns override, restoring the shared `.relationship-item` default for this list too; restore the single `.order-status-group`-wrapped cell (milestone badge + exception badge together) in the accordion row template, dropping the separate Exception status cell and its "None" fallback; restore the `.order-status-group` CSS rule. | Explicit user instruction, 2026-09-04, on the same screenshot: "please add a row specifiying what is is... SO/STO - Ship-to - Milestone - Exception Status. The distance between these should be equal please" |
| LS-D078 | 2026-09-04 | Fixes two defects behind "i dont see the button to go back at the top": (1) `.app-header`'s z-index (1100, set when LS-D064 built the v0.24 header) was higher than `.drawer-backdrop`/`.detail-drawer`/`.exception-drawer`'s (1000/1001/1002, unchanged since before that redesign), so the app's own topbar rendered on top of the drawer's own header for the first 56px of the viewport, hiding the drawer's title and close button behind it whenever a trip or order detail was open. The three drawer-related z-indices move to 1150/1151/1152, above the header. (2) `.detail-drawer` had no `overflow`, and `.drawer-scroll` (its scrollable body, everything below the header) had no `flex`/`min-height` constraint; in a fixed-height flex column this let the body's content grow the whole drawer taller than its own `height:100vh` instead of clipping and scrolling internally, which independently could also carry the header out of view. `.detail-drawer` gains `overflow: hidden`, and `.drawer-scroll` gains `flex: 1; min-height: 0;`, so only the body scrolls and `.drawer-header` (title, route line, close button) now always stays pinned at the top of the drawer regardless of how far the body is scrolled. Verified directly (headless browser, `?trip=Trip-A`, viewport top-left corner): "Trip-A" and the × close button render fully visible immediately below the app topbar; scrolling the drawer body to its Exception history section leaves the header still pinned. | `.drawer-backdrop`/`.detail-drawer`/`.exception-drawer` z-index 1000/1001/1002, below `.app-header`'s 1100. `.detail-drawer` had no `overflow` and `.drawer-scroll` had no `flex`/`min-height`. | Restore the three z-index values to 1000/1001/1002; remove `overflow: hidden` from `.detail-drawer`; remove `flex: 1; min-height: 0;` from `.drawer-scroll`. | Explicit user instruction, 2026-09-04, on a screenshot showing the drawer scrolled to its Journey section with no header or close button visible: "please adjust the size, i dont see the button to go back at the top" |
| LS-D079 | 2026-09-04 | Two follow-up refinements to LS-D077's "Orders carried on this trip" table, from a screenshot. (1) The Ship-to column drops `shipToNote`, the `<span class="subtext">Different ship-to than the trip's primary, ...</span>` line: the ship-to name alone is enough, per the user ("ship-to is enoigh, dont describe anything"). `legNote` (the multi-leg "Continues past X" / "Delivered at an earlier stop" line, used by `multiLegCases` orders elsewhere, not by any of Trip-A's orders) is untouched, since it wasn't what was shown or objected to. (2) SO-A4 gains an authored `exceptionHistory` (`POD not received`, status Snoozed, with a note: "coordinator confirmed the POD will be signed once the driver returns from the next drop"), so it reads Snoozed instead of falling back to `exceptionHistoryFor`'s Open-only default; SO-A4 was flagged in the screenshot as wrongly showing Open. This also now exercises LS-D076's Open-then-Snoozed-then-Resolved sort with a real Snoozed row for the first time in Trip-A's Exception history: SO-A3 (Open) is first, SO-A4 (Snoozed) second, Route deviation (Resolved) last. Verified directly (headless browser, `?trip=Trip-A`): the Ship-to column shows plain names only across all five rows; SO-A4's row and its Exception history entry both read "Snoozed" (gray badge); the three-entry list orders Open, Snoozed, Resolved top to bottom. | The Ship-to column showed a "Different ship-to than the trip's primary, Nhon Trach Distributor" subtext under four of the five orders. SO-A4 had no authored `exceptionHistory`, so it fell back to a single synthetic Open entry, same as SO-A3. | Restore the `shipToNote` variable and its `${shipToNote}` interpolation in the accordion row's Ship-to cell; remove the `exceptionHistory` array from SO-A4's order object, reverting it to the `exception`/`exceptionLevel` fallback (Open). | Explicit user instruction, 2026-09-04, on a screenshot of the updated "Orders carried on this trip" table: "please, ship-to is enoigh, dont describe anything. the SO-A4 should be snnozed, not opened" |
| LS-D080 | 2026-09-04 | Two fixes, from a screenshot of TR-4127's Exception history showing "Late arrival predicted" three times (once per carried order, SO-260825-1048/STO-260825-1309/STO-260825-1315, all sharing that exception via `sharedTripGroups`/`tripOwnedFields`, LS-D002). (1) TR-4127 gains a `tripExceptionHistory` entry for "Late arrival predicted" (on its anchor order, SO-260825-1048, mirroring Trip-A's Route deviation pattern from LS-D075), applying the same trip-owned-single-entry rule to a second, pre-existing trip rather than only the illustrative Trip-A. `renderExceptionHistory`'s order-entries aggregation (the `!isOrders` branch only) now filters out any order-level entry whose `type` is already present in `tripExceptionHistory`, so all three orders' independently-synthesized "Late arrival predicted" fallback entries (and the anchor's own authored one) collapse to the single trip-owned entry in the trip drawer's combined view; each order's own drawer and its own Orders-table row are unaffected, since they read `exceptionHistoryFor`/`exception` directly, never through this filtered aggregation. TR-4127's Exception history now reads "2 exceptions" (Late arrival predicted, Open; Route deviation, Resolved, still tagged to SO-260825-1048 since only its current, not historical, exception is trip-shared) instead of 4. (2) The badge and action-button columns in every `.relationship-item` (used by Exception history rows) now line up at the same x position across rows: the base rule's third and fourth grid tracks change from `minmax(150px,.8fr)`/`auto` to fixed `130px`/`235px`. Each `.relationship-item` is an independent grid (stacked divs, not real table rows sharing tracks), so the `auto` actions column previously sized itself per row by how many buttons that particular row had (2, 1, or 0, e.g. after LS-D076's conditional "Open the exception" button), silently shifting the status badge sideways row to row; fixed widths remove that dependency. Scoped to the base rule only, so LS-D077's `#trip-orders-list`-specific override (a different column shape) is untouched. Verified directly (headless browser, `?trip=TR-4127` and `?trip=Trip-A`): TR-4127's Exception history shows exactly 2 rows (no order-tagged duplicates of "Late arrival predicted"); in both trips' Exception history lists, every row's status badge and action button(s) start at an identical x position regardless of button count. | TR-4127's Exception history listed "Late arrival predicted" four times total (SO-260825-1048's own entry plus one synthesized fallback entry per sibling order) alongside "Route deviation" once. The base `.relationship-item`'s third/fourth columns were `minmax(150px,.8fr)`/`auto`, so each row (in Exception history, or anywhere else using the unscoped class) sized its own status/actions columns independently, based on its own content, drifting out of alignment with sibling rows whenever button count differed. | Remove `tripExceptionHistory` from SO-260825-1048 and the `tripTypes`/`.filter(entry => !tripTypes.has(entry.type))` logic from `renderExceptionHistory`, restoring the plain per-order flatMap; restore `.relationship-item`'s third/fourth grid tracks to `minmax(150px,.8fr)`/`auto`. | Explicit user instruction, 2026-09-04, on a screenshot of TR-4127's Exception history: "late arrival predicted should be related to the Trip, not the order, therefore, it should not duplicate the same problem. And why is it no vertiacally align?" |
| LS-D081 | 2026-09-04 | The multi-drop journey markers ("Arrived at Ship-to" and "POD Completed" on a multi-drop trip's Journey card, LS-D055) switch from a translucent whole-circle opacity tint to a hard-edge horizontal fill matching the fraction reached: `background:linear-gradient(to right, #315f89 X%, white X%)` (`X` = the rounded percentage, e.g. 80% for "4 of 5"), both stops fully opaque. The previous `rgba(49,95,137, fraction)` fill let the connecting `.journey-progress-line` (same blue, just behind the marker in z-index) show through the marker's own translucency, reading as a muddy, inconsistently-toned circle rather than a clean progress indicator; a fully opaque fill removes that regardless of z-index. The checkmark text color, previously forced `transparent` at every fraction including 1 (fully done), now shows `white` at fraction 1 to match the "done" step's own convention, a latent defect fixed as a side effect of rewriting this line. Verified directly (headless browser, `getBoundingClientRect` to pinpoint the two 20x20px marker nodes for a precise crop, since a loosely-cropped low-resolution view had made both markers misleadingly look like an even 50/50 split): the computed inline `style` attribute reads exactly `80%`/`80%` and `40%`/`40%` for the two gradient stops on "Arrived at Ship-to" (4 of 5) and "POD Completed" (2 of 5) respectively, and the rendered markers show a majority-filled circle with a small white crescent for the former and a minority-filled circle for the latter, with no line visible through either. | The fill was `background:rgba(49,95,137,${Math.max(.22, fraction)})`, a translucent tint of the same blue as the connecting line, and the node's `color` was hardcoded `transparent` regardless of fraction, so a fully-done (fraction 1) multi-drop marker never showed its checkmark. | Restore `const fill = fraction > 0 ? `background:rgba(49,95,137,${Math.max(.22, fraction).toFixed(2)});border-color:#315f89;color:transparent;` : "";`, removing `fillPercent` and the linear-gradient string, and removing the `fraction === 1 ? "white" : "transparent"` conditional. | Explicit user instruction, 2026-09-04, on a screenshot of the Journey card: "Instead of filling opacity, which looks every weird, please make the partial fill instead like if it is 4/5, the fill of the circle should be 4/5 horizontally... And the dot should de the line, not seeing the line behind tho, like it is on the front layer" |
| LS-D082 | 2026-09-04 | Two follow-ups sent mid-review of Trip-A's drawer. (1) Supersedes the Exception-status column added to "Orders carried on this trip" by LS-D077: it is removed, on the user's own reconsideration that the Exception history card below already covers it ("I think the Exception history is enough"). The list returns to three equal-width columns (SO/STO, Ship-to, Milestone) instead of four; the `latestExceptionStatus`/badge computation per row in `openTripDrawer` is removed along with the "Exception status" header label, and the shared grid (`#trip-orders-list .relationship-item, .order-carried-columns`) drops its fourth `minmax(0,1fr)` track. The now-fully-unused `.order-status-group` CSS rule (already unreferenced in markup since LS-D077 split it into two cells, never cleaned up) is also removed. (2) Trip-A's `route` field (on its canonical first order, SO-A5, and the identical `tripJourney` route string on all five of its orders) changes from `"OPMT → 5 drops (Thu Duc, Di An, Bien Hoa, Long Thanh, Nhon Trach)"` to `"OPMT → Nhon Trach Distributor"`, matching the plain origin-to-final-destination format every other trip's route uses, rather than enumerating every intermediate drop. Each individual order's own `route` field (e.g. SO-A1's "OPMT → Thu Duc") is untouched, since those correctly describe that one order's own leg; only the trip-level route string (shown in the Trips table's Route column and the drawer's route subtitle) changes. Verified directly (headless browser): "Orders carried on this trip" shows exactly three header columns and three data columns per row; the Trips table's Trip-A row and its drawer subtitle both read "OPMT → Nhon Trach Distributor". | The "Orders carried on this trip" list had four columns, the fourth showing each order's exception status badge (LS-D077/LS-D079). Trip-A's trip-level route read "OPMT → 5 drops (Thu Duc, Di An, Bien Hoa, Long Thanh, Nhon Trach)" in the Trips table and the drawer subtitle. | Restore the `orderExceptionStatus`/`orderExceptionBadge` block and the fourth `<span>` cell in the accordion row template (`openTripDrawer`); restore "Exception status" to the header row; restore the fourth `minmax(0,1fr)` track and the `.order-status-group` rule. Restore SO-A5's `route` field and all five `tripJourney` route strings to `"OPMT → 5 drops (Thu Duc, Di An, Bien Hoa, Long Thanh, Nhon Trach)"` / `"OPMT → 5 drops"` respectively. | Explicit user instruction, 2026-09-04, sent mid-turn with two screenshots: "Remove the Exception Status inside the Orders carried on this trip, I think the Exception history is enough" and "Route should not look like this, only show one point to one point pleasse, dont show drop" |
| LS-D083 | 2026-09-04 | A multi-drop trip's "Live journey map" (`renderDetailMap`) now shows one numbered marker per intermediate ship-to, not only the origin, current position and final destination it showed before. For each of `entity.orders` whose own `coords.destination` differs from the trip's overall destination (the final drop, which already has the "D" marker at that same point, so it is skipped to avoid an exact duplicate), a small (22px, vs the 28px O/current/D markers) numbered pin is added: green (`.map-marker.drop-done`) if that order's `current` is "Arrived at Ship-to" or "POD Completed", muted gray (`.map-marker.drop-pending`) otherwise, with a popup naming the ship-to and its drop number. The map's fit-bounds calculation is extended to include every drop's coordinates, not just the travelled/remaining polyline points, so all markers stay in frame. The marker number itself is parsed from that order's own `currentPlace` text (`/drop (\d+) of \d+/`, e.g. "Thu Duc Distributor, drop 1 of 5"), not from its position in the `orders` array: Trip-A's `orders` array starts with its drop-5 (in-transit) order first, so buildTrips can derive the trip's own summary fields from it (LS-D055); numbering by array position instead would have mislabeled the four markers 2, 3, 4, 5 (skipping 1) instead of 1, 2, 3, 4. Verified directly (headless browser, `?trip=Trip-A`): four green numbered markers (1 Thu Duc, 2 Di An, 3 Bien Hoa, 4 Long Thanh) render along the route in the correct geographic and sequential order, all in view alongside the O/current/D markers. | The detail map showed only the origin, current position and final destination markers for every trip, including a multi-drop one; none of Trip-A's four earlier drops (Thu Duc, Di An, Bien Hoa, Long Thanh) had any marker of their own. | Remove the `isMultiDropMap`/`dropOrders` block (both the marker-adding `forEach` and the `bounds.extend` line) from `renderDetailMap`; remove the `.map-marker.drop`/`.drop-done`/`.drop-pending` CSS rules. | Explicit user instruction, 2026-09-04, on a screenshot of Trip-A's Live journey map: "for multip-drop on the trip view, please add the location of the drop location also okay?" |
| LS-D084 | 2026-09-04 | `resolveRoute` gains a third branch, applying generically to any multi-drop trip (not hardcoded to Trip-A, per the user's own follow-up: "apply the same thing for every other multi-drop trip"), condition `Array.isArray(entity.orders) && new Set(entity.orders.map(m => m.customer)).size > 1` (the same test LS-D083's marker logic uses). Previously every entity, multi-drop trips included, got one OSRM road-route call straight from `entity.coords.origin` to `entity.coords.current`, ignoring the hand-authored waypoints in `entity.travelled` (each drop's own coordinate) entirely; OSRM's shortest real road path between just those two endpoints has no reason to pass near any intermediate drop, which is why LS-D083's numbered markers rendered visibly off the drawn line ("why is it not going through"). The new branch instead calls `fetchRoadSegment` once per consecutive pair in `entity.travelled` (origin to drop 1, drop 1 to drop 2, ... to the current position), sequentially awaited to stay easy on the public OSRM demo endpoint, then concatenates the legs the same way the existing Ship-mode branch already does (`leg.slice(1)` on every leg but the first, since consecutive legs share an endpoint). Non-multi-drop entities (every other trip, and any single order's own drawer, multi-drop or not) are unaffected, still one direct origin-to-current call. Verified directly (headless browser, `?trip=Trip-A`, a higher `--virtual-time-budget` to allow the now-five sequential OSRM calls to resolve): the drawn travelled line visibly threads through drop markers 1, 2 and 3 in a zoomed crop, following real road geometry leg to leg rather than passing north of them. | `resolveRoute`'s non-Ship branch called `fetchRoadSegment(entity.coords.origin, entity.coords.current)` for every entity, multi-drop trips included, so a multi-drop trip's drawn line was one continuous OSRM-computed road route between just those two endpoints, unrelated to where its own drops actually sat. | Remove the added `else if` branch (the `waypoints`/`legs`/`for` loop and the `reduce` concatenation), restoring the plain two-branch `if (entity.mapMode === "Ship") ... else { travelled = await fetchRoadSegment(entity.coords.origin, entity.coords.current); }`. | Explicit user instruction, 2026-09-04, on a screenshot of Trip-A's map showing the route passing north of drop markers 1-3: "why is it not going through?", followed by "apply the same thing for every other multi-drop trip please" |
| LS-D085 | 2026-09-04 | Generalizes LS-D080's per-trip fix (TR-4127's "Late arrival predicted") to every `sharedTripGroups` trip, on the user's own follow-up after spotting the identical problem on a second trip (TR-4151's "Dock waiting exceeds threshold", duplicated across its three orders): `sharedTripGroups.forEach` now derives `anchor.tripExceptionHistory` automatically, right after copying `tripOwnedFields` (which already includes `exception`/`exceptionLevel`, the exact fields this is about) onto every member. For any anchor whose current `exception` is not "None", it finds the entry in `exceptionHistoryFor(anchor)` whose `type` matches that current exception and sets it as `anchor.tripExceptionHistory`, so the existing type-matching filter in `renderExceptionHistory` (LS-D080) collapses every member's independently-synthesized duplicate down to that one entry, for every affected trip at once. TR-4127's hand-authored `tripExceptionHistory` (added by LS-D080) is removed, now redundant: the generic derivation produces the identical entry from the same `exceptionHistory` array already on that order. This fixed two more trips beyond TR-4127: TR-4151 ("Dock waiting exceeds threshold", 3 orders collapsed to 1 exception) and TR-4160 ("No transporter accepts the trip", 2 orders collapsed to 1 exception); `sharedTripGroups`' fourth group (TR-4142) has no current exception on its anchor, so nothing to derive there, correctly unaffected. Verified directly (headless browser, `?trip=TR-4151`, `?trip=TR-4160`, `?trip=TR-4127`): each Exception history card now reads "1 exception" (or "2" for TR-4127, its Route deviation plus the derived Late arrival predicted) instead of the previous 3/2/duplicated counts, and TR-4127's card is pixel-identical to before the hand-authored entry was removed, confirming the generic derivation is a true drop-in replacement. | TR-4151's Exception history listed "Dock waiting exceeds threshold" three times (once per carried order); TR-4160's listed "No transporter accepts the trip" twice. Only TR-4127 had been fixed, via a hand-authored `tripExceptionHistory` specific to that one trip (LS-D080), which did not generalize to any other `sharedTripGroups` trip with a current exception. | Remove the `if (anchor.exception !== "None") { ... }` block from `sharedTripGroups.forEach`; restore SO-260825-1048's hand-authored `tripExceptionHistory: [{ type: "Late arrival predicted", ... }]` field (LS-D080's original). | Explicit user instruction, 2026-09-04, on a screenshot of TR-4151's Exception history showing "Dock waiting exceeds threshold" three times: "duplicating exception for the order while it is on the trip? please, apply the same logic for other trip please... apply the same logic please, FIX THAT" |
| LS-D086 | 2026-09-04 | Five changes to exception presentation, all from one round of screenshots. (1) Open is always the amber badge now, never red or violet: `exceptionCell` and `exceptionStatusClass` both drop the `entity.exceptionLevel`/`entry.level` branch for a non-Resolved, non-Snoozed status, using `"amber"` unconditionally, so severity no longer changes an Open badge's color (only its status does). (2) The Exception column's per-row subtext (the exception's own name, e.g. "Late arrival predicted", plus any "+N more exceptions" count) is removed from `exceptionCell`; the column is now the status badge alone. (3) A Lana AI summary card, matched directly to the master mockup's own `.aisum`/`.ah` component (`UI/YCH_TCT_SPVB_v24.html`, "Summarised by Lana"), is added to both drawers between the Journey card and the map: a new `combinedExceptionEntries(entity, isOrders)` helper (factored out of `renderExceptionHistory`, which now calls it too, so the two always agree) feeds `lanaSummaryFor`, a templated paragraph (not a live model call - no such backend exists for this pod) naming the entity, its exception count and how many are still open, and narrating the single most urgent entry (type, order if any, status, raised time, note). The card (`#lana-summary`, a "Read more" toggle expanding the full text as an overlay, same interaction as master's) is omitted entirely when there are no exceptions to summarize, and hidden for the SPVB/customer role alongside the existing Exception history card, since exception detail is not customer-visible. (4) Both the Trips and Orders tables now sort Open-first by default: `filteredEntities` applies `exceptionSortRank` (Open, then Snoozed, then Resolved, then none) whenever `state.sortKey` is unset, falling back to the explicit column sort once a user clicks a header; the sort is stable, so Trip-A (already unshifted to the front of the data, LS-D074) stays first among the other Open entities. (5) An entity with no open exception now reads "On plan" instead of "None" in the Exception column. Verified directly (headless browser): the default Trips table opens with every Open-status trip (Trip-A first) before any "On plan" trip, all Open badges rendering the same amber, no subtext under any of them; Trip-A's and SO-A3's drawers both show the Lana card with a correct narrative, its "Read more" expanding without shifting the map below; TR-4142's drawer (no exception) and the SPVB role's SO-A3 drawer both correctly show no Lana card at all. | Open badges took their color from `exceptionLevel` (amber, red or violet). The Exception column showed the exception's name as subtext under the badge. No Lana summary existed in either drawer. Both tables rendered in plain array/insertion order by default, so an Open-status entity could sit anywhere among "None" ones. A row with no exception read "None". | Restore `entity.exceptionLevel`/`entry.level` in place of the hardcoded `"amber"` in both `exceptionCell` and `exceptionStatusClass`; restore the `<span class="subtext">...</span>` (exception name plus more-count) in `exceptionCell`'s return; remove the `#lana-summary` markup and its CSS, `lanaSummaryFor`, `renderLanaSummary`, the `combinedExceptionEntries` extraction (inlining it back into `renderExceptionHistory`), and the two `renderLanaSummary`/hide calls in `openOrderDrawer`/`openTripDrawer`; remove the `isOrders`/`exceptionSortRank` default-sort branch from `filteredEntities`, restoring the plain `applySort(...)` call; restore `'<span class="none">None</span>'` in `exceptionCell`. | Explicit user instruction, 2026-09-04, on screenshots of the Exception column, the Trips table, and a "Summarised by Lana" reference card: "opened should only have one color: YELLOW", "No description should be made in the exception column", "Add a Lana AI section where summary of exceptions OF THAT TRIP... are described... put it above the map, under the Journey", "put the trip/order with Open on top first", "instead of writing none, please write on plan" |
| LS-D087 | 2026-09-04 | Simplifies LS-D086's Lana AI summary card: the clamp/"Read more" expansion is removed, on the user's own follow-up ("remove the read more on Lana AI please, this is one example") after seeing it was unnecessary for a summary this short. `<p class="clamp" id="lana-summary-clamp">`, the `.more` "Read more" button and the absolutely-positioned `.full` overlay div are replaced with one plain `<p id="lana-summary-text">`; `renderLanaSummary` now sets that paragraph's text directly (`setText("lana-summary-text", summary)`) instead of populating both a clamped and a full copy; the `#lana-summary-toggle` click listener and the `.aisum p.clamp`/`.more`/`.full`/`.open .full` CSS rules are removed along with `.aisum`'s `position: relative` (no longer needed without an absolutely-positioned child). The card still matches the master mockup's `.aisum`/`.ah` header styling and still omits itself entirely when there is nothing to summarize, or for the SPVB role; only the always-two-line-then-expand interaction is gone, since the full paragraph now renders in place from the start. Verified directly (headless browser, `?trip=Trip-A`): the card shows the complete narrative with no "Read more" link and no overlay, and both drawer script blocks pass `node --check`. | The card showed only the first two lines of the summary (CSS line-clamp) with a "Read more" button that toggled an absolutely-positioned overlay carrying the full text. | Restore the `<p class="clamp" id="lana-summary-clamp">`/`<button class="more" id="lana-summary-toggle">Read more</button>`/`<div class="full" id="lana-summary-full">` markup; restore `renderLanaSummary`'s two `setText` calls (`-clamp` and `-full`) and its `card.classList.remove("open")` line; restore the `#lana-summary-toggle` click listener (`classList.toggle("open")`); restore `.aisum`'s `position: relative` and the `p.clamp`/`.more`/`.full`/`.open .full` CSS rules. | Explicit user instruction, 2026-09-04, on a screenshot of the expanded Lana AI card: "remove the read more on Lana AI please, this is one example" |
